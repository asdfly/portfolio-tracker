# 产品交接评估 — portfolio_tracker

评估人：许清楚（产品经理）｜评估日期：2026-08-05｜方法：实际读码 + 数据库实查，不依赖文档自述

---

## 一、结论速览（RoleVerdict）

**verdict：GO（可接手）** — 成熟度高于典型「交接项目」。功能与文档一致，零 TODO 残留，数据链路活跃。但存在 1 个核心价值未兑现的产品缺口，需在接手首周处理。

---

## 二、当前功能清单（代码核实）

`dashboard.py:1078` 的 `TAB_REGISTRY` 注册 **15 个 Tab**，经 `zip(tab_objects, TAB_REGISTRY)` 懒加载渲染，全部可达：

| # | Tab | 状态 |
|---|-----|------|
| 1–2 | 净值走势 / 持仓分布 | 已上线 |
| 3 | 风险分析（含 alerts/attribution/warnings/dashboard 四个子模块） | 已上线 |
| 4–7 | 收益日历 / 高级分析 / 技术信号 / 资讯与评估 | 已上线 |
| 8 | 操作建议（13 维度引擎 + 反馈追踪面板） | 已上线，闭环未启用 |
| 9–12 | 自定义指标 / 资金动向 / 黄金市场（10 子 Tab） / 宏观市场 | 已上线 |
| 13–15 | 数据质量 / 市场事件 / 交易复盘 | 已上线 |

README 描述与代码实际注册**完全一致**，无虚标功能。支撑代码：`src/` 55 个模块、`tests/` 78 个测试文件。

---

## 三、产品缺口与残留

**扫描结果：`src/`、`tabs/`、根目录 Python 文件中 TODO / FIXME / 未实现 / 占位 计 0 处。** 无空 Tab、无 stub 渲染函数。

实际缺口如下：

### P0：操作建议闭环 0% 兑现（最重要发现）

`advice_history` 共 **1,841 行，status 全部为 pending**；`action_taken` 非空 0 条、`feedback` 非空 0 条、`resolved_at` 非空 0 条。

闭环 UI 是**存在**的（`tabs/tab8_advice.py:1246 _render_feedback_tracking`，更新语句在 `:1354`），但从未被使用过。这意味着：系统每天产出建议（最新一批 2026-08-05 10:16，类型分布 opportunity 913 / caution 527 / risk_mgmt 239 / rebalance 162），却**无法回答「这些建议准不准」**。产品最核心的差异化——建议引擎——目前没有任何命中率证据支撑。推测原因：入口埋在 tab8 第 1246 行、需逐条手动标记，操作成本过高。

### P1：僵尸表与采而未存

- `research_reports`（0 行）：全仓库**零代码引用**，纯死 schema
- `trades`（0 行）：零引用，已被 `trade_records`（1,157 行）取代
- `_migration_version`（0 行）：零引用，迁移版本追踪从未初始化
- `src/data_sources/research.py:181 fetch_research_reports()` 返回 DataFrame 但**不落库**，属「采而未存」

### P1：仓库卫生

- 84MB 的 `portfolio.db` 被提交进 Git（commit 24574df），且每次运行都产生 diff（当前 `git status` 已显示 modified）→ 仓库膨胀 + 提交噪声（归 DevOps）
- 根目录 32 个调试 PNG（未被 Git 跟踪，纯本地残留）
- 根目录 4 个游离 `.db`：`portfolio.db` / `portfolio_data.db` / `portfolio_records.db` 均为 0 字节，`portfolio_tracker.db` 为 300KB 孤儿库。**权威库是 `data/database/portfolio.db`**——新接手人极易连错空库，是最隐蔽的踩坑点
- `tabs/__init__.py` 只导出 14 个 render 函数，缺 `render_tab15`（因用懒加载 `__import__` 故无功能影响，仅一致性瑕疵）

### P1：图标方案不符团队规范

15 个 Tab 标签及 tab8 状态标签均以 emoji 作功能图标，与团队 P0 规则冲突，建议统一替换为 SVG 图标库（选型由架构师锁定）。

---

## 四、数据覆盖度

数据库 **30 张表 / 457,270 行**（已超既往记录的 32 万，实为 45.7 万）。

**三张曾闲置大表现已全部被消费，D1 确认落地：**

| 表 | 行数 | 消费路径（代码证据） |
|----|------|---------------------|
| stock_margin | 211,826 | `advisor.py:112` 调用 `_analyze_margin_data` → 生成建议；另接 `market_event_signals.py:108`、tab14、data_quality |
| stock_institution_research | 20,159 | `advisor.py:117` 调用 `_analyze_institution_research`；`market_event_signals.py:57`；tab14 |
| stock_holder_change | 3,741 | `market_event_signals.py:142`、`tab14_market_events.py:263` |

生产侧佐证：2026-08-05 最新建议中包含「机构调研热度骤增标的」，说明链路真实跑通，非仅代码存在。回归测试位于 `tests/test_d1_margin_research_block.py`。

**数据新鲜度健康**：portfolio_snapshots / etf_technical / daily_news / fund_flows 均至 08-04；macro_daily 与 advice_history 至 08-05 当日；市场事件类 T-1 至 08-03，属正常采集节奏。无断更迹象。

**闲置项**：仅剩上文 3 张空表 + `fetch_research_reports` 未落库。

---

## 五、接手后优先级建议

判断依据：新接手人第一周最可能问的两个问题是「我能不能信这套建议」和「我会不会连错数据」。

| 优先级 | 事项 | 理由 |
|--------|------|------|
| P0-1 | 激活建议闭环：把标记入口前置到建议卡片行内（一键「已执行/已忽略」），并新增命中率看板 | 1,841 条建议 0 反馈 = 核心功能无法自证价值；不解决则后续所有引擎优化都没有评价基准 |
| P0-2 | 明确唯一数据源：删除根目录 4 个游离 .db，README 顶部标注权威路径 | 防止新接手人连空库后误判「系统没数据」 |
| P0-3 | 将 84MB DB 移出 Git 跟踪，改为备份产物 | 阻止仓库持续膨胀（与 DevOps 协同） |
| P1-1 | 清理 3 张僵尸表；`fetch_research_reports` 要么落库接入 tab7，要么删除 | 消除「有表无数据」对数据质量 Tab 评分的干扰 |
| P1-2 | Tab 图标 emoji → SVG 图标库 | 团队 P0 规范对齐 |
| P1-3 | 根目录 32 个 PNG 清理；`tabs/__init__.py` 补 `render_tab15` | 降低新人认知噪声 |

---

## 六、RoleVerdict

- **verdict**：GO — 项目可安全接手。15 个 Tab 全部真实可用，零 TODO 残留，D1 闲置数据激活已确证落地，数据链路每日活跃。
- **blocking**：
  1. 操作建议闭环 0% 兑现（1,841 条 pending / 0 反馈），核心价值无法验证
  2. 根目录 4 个游离 .db 造成权威数据源歧义，新接手人有连错空库风险
- **advisory**：
  1. 84MB DB 入 Git，仓库膨胀且每次运行产生 diff（DevOps）
  2. 僵尸表 research_reports / trades / _migration_version 零引用
  3. `fetch_research_reports` 采而未存
  4. 15 个 Tab 以 emoji 作图标，违反团队图标规范
  5. `tabs/__init__.py` 漏导出 render_tab15（无功能影响）
- **evidence**：
  - `dashboard.py:1078-1094` TAB_REGISTRY 15 项
  - `advisor.py:112 / :117` 三大表消费调用点；`market_event_signals.py:108/142/200`
  - `tab14_market_events.py:263`；`tests/test_d1_margin_research_block.py`
  - DB 实查：30 表 / 457,270 行；stock_margin 211,826、stock_institution_research 20,159、stock_holder_change 3,741
  - `advice_history` 1,841 行全 pending，三个闭环字段填充率均为 0
  - `tabs/tab8_advice.py:1246 / :1354` 闭环 UI 与 UPDATE 语句存在但未启用
  - TODO/FIXME 全量扫描结果 0
  - git commit 24574df「提交数据库文件 portfolio.db (84MB)」
