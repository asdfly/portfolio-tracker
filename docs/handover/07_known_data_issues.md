# 07 · 已知数据问题与口径陷阱

> 记录日期：2026-08-26
> 状态：**✅ 已修复**（2026-09-03 随提交落地：代码正则 + 生产库脏数据双修，见各节"✅ 已修复"标注）
> 证据获取方式：对 `data/database/portfolio.db` 以 `file:...?mode=ro` 只读连接执行 SQL，未做任何写入。

本文档登记交接评估期间实测发现的数据正确性问题。这些问题**不影响现有功能运行**（不会报错、不会崩溃），但会让统计口径失真、并可能污染下游分析。因为影响面是"数字不对"而非"跑不起来"，容易长期潜伏，故单列成篇。

---

## 问题一：`_is_etf` 代码正则存在假阳性，把场外基金判成 ETF

### 位置

`src/analysis/predictor/build_base.py:23-32`

```python
_ETF_CODE_RE = re.compile(r"^(5\d{5}|1[56]\d{4})$")

def _is_etf(code: str, name: str) -> bool:
    """判定持仓记录是否为 ETF：name 含 'ETF' 或代码符合 ETF 模式。"""
    if name and "ETF" in str(name).upper():
        return True
    if code and _ETF_CODE_RE.match(code):
        return True
    return False
```

### 问题

`5\d{5}` 会吞掉整个 `5xxxxx` 段，但 **`519xxx` 是场外开放式基金代码段**，不是场内 ETF。同理 `1[56]\d{4}` 会吞掉 `166xxx`（LOF / 分级基金）。

实测对全历史 36 只标的逐一判定，正则与"名称含 ETF"两种判据冲突 3 例：

| 代码 | 名称 | 正则判定 | 名称判定 | 真实身份 | 结论 |
|------|------|---------|---------|---------|------|
| `166301` | 华商新趋势优选灵活配置混合型证券投资基 | ✅ ETF | ❌ | 混合型 LOF | **正则假阳性** |
| `519770` | 交银优择回报灵活配置混合A | ✅ ETF | ❌ | 场外混合基金 | **正则假阳性** |
| `512810` | 国防军工 | ✅ ETF | ❌ | 华宝中证军工 ETF | **名称判据假阴性**（库里存简称，无 "ETF" 字样） |

因为 `_is_etf` 是 `name` 判据与 `code` 判据的**或**关系，`512810` 靠正则救回来了（结果正确），但 `166301` / `519770` 被正则错误放行（结果错误）。

### 实际影响：目前为零，但是"侥幸"

`resolve_target_codes`（同文件 L35+）只对**最新持仓快照**跑 `_is_etf`。当前持仓 22 只已全为真 ETF，`166301` / `519770` 早已清仓、不在最新快照里，所以两个假阳性**当前没有进入预测底座**。

这是运气，不是设计。一旦这两只（或任何 `519xxx` / `166xxx` 场外基金）被重新买入并出现在最新快照，它们就会：

1. 被纳入 `resolve_target_codes` 的目标域；
2. 触发 `backfill_etf_price_history` 去东方财富 / 新浪拉**场内 ETF 日 K**——而场外基金没有场内日 K，采集会失败或拉到空数据；
3. 进而在 `etf_features` / `etf_forward_returns` 里产生残缺行，污染风险模型训练集。

### 建议修法

不要继续在正则上打补丁（代码段规则会变）。改为**以配置为准**：

- 首选：判定改为 `code in config.settings.ETF_CATEGORIES`（实测该配置全集 = 23 只 = `etf_technical` 集合，是干净的权威源）。
- 若必须保留正则兜底，至少排除已知场外段：`519xxx`、`166xxx`，即把 `5\d{5}` 收紧为 `5(0|1|2|3|5|6|8)\d{4}` 之类的白名单式写法，并补单元测试固定住 `166301` / `519770` / `512810` 三个样本的期望判定。

### 建议回归用例

```python
# 三个真实样本，把两类错误都钉住
assert _is_etf("512810", "国防军工") is True       # 真 ETF，名称无 ETF 字样
assert _is_etf("166301", "华商新趋势优选混合") is False  # LOF，勿判 ETF
assert _is_etf("519770", "交银优择回报混合A") is False   # 场外，勿判 ETF
```

### ✅ 已修复（2026-09-03，随提交落地）

未采用"查 `config.ETF_CATEGORIES`"方案，改用**收紧正则**：

```python
_ETF_CODE_RE = re.compile(r"^(?!166|519)(5\d{5}|1[56]\d{4})$")
```

理由：查表会把 `512810`（华宝中证军工 ETF，库内存简称"国防军工"、无 "ETF" 字样、且**不在** `config.ETF_CATEGORIES` 代码列表里）误杀——它会因 name 无 ETF、code 不在表而被判非 ETF，反而漏掉一只真 ETF。收紧正则保留 `5\d{5}` 兜住 `512810`，同时用负向前瞻 `(?!166|519)` 排除 `166xxx`(LOF) / `519xxx`(场外) 两个已知场外段，`name含ETF` 仍为主信号。9 例验证全过（含 `512810`→True、`166301`/`519770`/`001323`/`002152`/`001194`→False）。

---

## 问题二：`etf_fundamental` 表混入 2 只场外混合基金

### 现状

`etf_fundamental` distinct `code` = **25**，比真实场内 ETF 全集（23）多出 2 只：

| 代码 | 名称 | `etf_fundamental` 行数 |
|------|------|----------------------|
| `001323` | 东吴移动互联混合A | 19 |
| `002152` | 华宝核心优势混合 | 19 |

这两只是**场外混合基金**，不该出现在一张名为 `etf_fundamental` 的表里。集合关系实测：

```
etf_fundamental - etf_technical = {001323, 002152}
etf_technical  - etf_fundamental = {} （空）
```

即 `etf_fundamental` ⊃ `etf_technical`，多出来的正好是这 2 只脏数据。

### 影响

- 任何直接 `SELECT COUNT(DISTINCT code) FROM etf_fundamental` 当作"ETF 覆盖数"的统计都会虚高 2 只（这正是交接文档一度把覆盖写成 25 的来源）。
- 若有分析逻辑遍历 `etf_fundamental` 做横向对比（如估值/规模排序），这 2 只场外基金会作为不可比标的混入结果。

### ✅ 已修复（2026-09-03，随提交落地）

1. **写入来源已定位且确认安全**：`etf_fundamental` 的写入函数是 `src/data_sources/etf_fundamental.py:445` 的 `save_to_db(conn, "etf_fundamental", ...)`，包裹在 `run_etf_fundamental_collection` 内；该函数**仅被 `scripts/verify_sina_whitelist.py` 调用，不在主采集链**（`build_base` / `run_analysis` 不碰它）。因此历史脏数据是早期一次性写入，不会在后续例行采集中被重新写回——可直接清库。
2. **已执行清理**（生产库 `data/database/portfolio.db`，非 git 跟踪；已备份验证）：
   ```sql
   DELETE FROM etf_fundamental WHERE code IN ('001323','002152');  -- 各 19 行，共 38 行
   ```
   distinct `code` 由 **25 → 23**，与 `etf_technical` / `config.ETF_CATEGORIES` 对齐。复查 `fund - tech == set()`（空集）。
3. 采集期过滤：写入前已可用修好的 `_is_etf`（见问题一）过一遍，杜绝再次混入。

---

## 附：ETF 数量口径速查（防止再次数错）

| 数字 | 含义 | 出处 | 可否作分母 |
|------|------|------|-----------|
| **36** | 全历史全部标的 = 23 场内 ETF + 13 场外标的 | `portfolio_snapshots` distinct `code` | ✅ 全标的口径 |
| **23** | 场内 ETF 全集 | `etf_technical` distinct `code`，实测 == `config/settings.py` 的 `ETF_CATEGORIES` | ✅ **推荐的 ETF 权威分母** |
| **22** | 预测底座 / 风险模型覆盖 | `etf_features`、`etf_price_history` | ✅ 模型覆盖口径（= 23 − 已清仓 `159732`） |
| **23** | `etf_fundamental` 标的数 | 2026-09-03 已清 `001323`/`002152` 脏数据，现与 `etf_technical` 对齐 | ✅ 可用 |
| **25** | 用代码正则判出的 ETF 数 | 含 2 只假阳性 `166301`/`519770` | ❌ **不可用**（与上一行数值相同但集合不同，纯属巧合） |
| **59** | `(code, name)` 去重对数 | 同一代码有简称/全称两种写法 | ❌ 不是标的数 |

> **59 的成因示例**：`159220` 在 `portfolio_snapshots` 中同时存在"港股通红利低波ETF华宝"与"港红利"两种 `name`。按 `(code,name)` 去重会把同一只 ETF 数成 2 只。历史上曾据此误报"48 只 ETF"，实为此类假象。

### 场外标的清单（13 只，供核对）

`001194` 景顺长城稳健回报灵活配置混合A、`001323` 东吴移动互联混合A、`001407` 景顺长城稳健回报灵活配置混合C、`001437` 易方达瑞享灵活配置混合I、`001765` 前海开源嘉鑫混合A类、`002152` 华宝核心优势混合、`007994` 华夏中证500指数增强A、`008269` 大成睿享混合A、`027293` 东吴产业趋势混合A、`100032` 富国中证红利指数增强前端、`880013` 天添利（现金管理类）、`166301` 华商新趋势优选混合（LOF）、`519770` 交银优择回报混合A。

---

## 复核脚本

以下脚本可随时重跑以验证本文档结论（**只读，不写库**）：

```python
import sqlite3, re
con = sqlite3.connect('file:data/database/portfolio.db?mode=ro', uri=True)
cur = con.cursor()
RE = re.compile(r"^(5\d{5}|1[56]\d{4})$")

snap = dict(cur.execute("SELECT code, MAX(name) FROM portfolio_snapshots GROUP BY code").fetchall())
tech = {r[0] for r in cur.execute("SELECT DISTINCT code FROM etf_technical")}
fund = {r[0] for r in cur.execute("SELECT DISTINCT code FROM etf_fundamental")}
feat = {r[0] for r in cur.execute("SELECT DISTINCT code FROM etf_features")}

regex_set = {c for c in snap if RE.match(c)}
name_set  = {c for c in snap if 'ETF' in (snap[c] or '').upper()}

assert len(snap) == 36
assert len(tech) == 23
assert regex_set - tech == {'166301', '519770'}   # 正则假阳性
assert tech - name_set == {'512810'}              # 名称判据假阴性
# 修复后：etf_fundamental 已清脏数据，fund - tech 应为空集
assert fund - tech == set()                         # etf_fundamental 脏数据已清理（2026-09-03）
assert tech - feat == {'159732'}                  # 已清仓，故预测底座少 1
print("全部结论复核通过")
```

---

## 优先级判定

| 问题 | 严重度 | 紧急度 | 理由 |
|------|--------|--------|------|
| 一 · `_is_etf` 假阳性 | 中 | 低 | 当前无实际影响（两只已清仓），但属**埋雷**：一旦买入同类场外基金即触发采集失败 + 训练集污染。建议随下次 predictor 模块改动一并修掉。 |
| 二 · `etf_fundamental` 脏数据 | 低 | 低 | 仅影响统计口径，不影响运行。**必须先定位写入来源再清库**，否则会被采集重新写回。 |
| 三 · `portfolio_nav.total_units` 错位 | 中 | 中 | 字段 100% 错误，当前无下游读取故影响为零，但任何新消费方都会踩坑（见下）。 |
| 四 · `portfolio_nav.mwr_return` 与 TWR 矛盾 | 中 | 中 | **用户可见**：该值已进入 `load_portfolio_nav()` 返回值，展示出来是 −73.5%。 |

前两者均**不构成上线/交接阻断项**，但应写入待办，不要遗忘。

---

## 问题三：`portfolio_nav.total_units` 存的是「前一个交易日的市值」

> 记录时间 2026-09-15 · 发现人 data-engineer · **状态：仅记录，未修复**
> 本轮同时在做「场外净值写入 portfolio_snapshots」，为避免事后分不清是哪个改动引起的，本 bug 不在本轮修复。

### 复现查询

```sql
SELECT date, total_units, total_value,
       LAG(total_value) OVER (ORDER BY date) AS prev_day_total_value
FROM portfolio_nav ORDER BY date DESC LIMIT 15;
```

| date | total_units | 当日 total_value | 前一日 total_value | 是否相等 |
|---|---|---|---|---|
| 2026-09-14 | 933294.5 | 933891.5 | 933294.5 | ✅ |
| 2026-09-11 | 945046.1 | 933294.5 | 945046.1 | ✅ |
| 2026-09-10 | 950179.2 | 945046.1 | 950179.2 | ✅ |
| 2026-09-09 | 950728.9 | 950179.2 | 950728.9 | ✅ |

```sql
-- 全表命中率
SELECT COUNT(*) FROM (
  SELECT total_units, LAG(total_value) OVER (ORDER BY date) pv FROM portfolio_nav
) WHERE pv IS NOT NULL AND ABS(total_units - pv) < 0.01;
-- => 3476   （全表 3477 行，唯一未命中的是首行，因为它没有前值）
```

**结论：3476 / 3477 = 99.97% 命中，是 100% 系统性错位，不是偶发。**

### 根因（源码行号）

`src/analysis/nav_engine.py:222-234`：

```python
rows.append({
    "date": d_str,
    "unit_nav": round(unit_nav, 6),
    "total_units": round(prev_v, 2),      # ← BUG：prev_v 是上一轮的 v
    "total_value": round(v, 2),
    ...
})
prev_v = v                                # ← 在 append 之后才推进
```

`prev_v` 在 `append` **之后**才被赋成当日 `v`，因此写入本行的 `total_units` 恒等于**前一个交易日**的 `total_value`。

### 影响范围（已核实）

- **当前影响 = 0**：全项目 grep `total_units` 只有 3 处，全是写入侧
  （`db_schema.py:448` 建表、`nav_engine.py:226` 赋值、`nav_engine.py:249/266` 建表与 INSERT）。
  `nav_engine.py:294` `load_portfolio_nav()` 的 SELECT 列里**没有** `total_units`，即无任何下游读取。
- **字段语义**（`db_schema.py:448` 注释）：`总份额（无申赎则恒定=初始市值）`。
  正确值应为 `total_value / unit_nav`；现状是滞后一天的市值，语义完全不对。
- **未受污染的部分**：`unit_nav` 是独立计算的（`nav_engine.py:221` `unit_nav = prev_nav * (1 + r_total)`），
  并非 `total_value / total_units`。实测 09-14：`total_value/total_units = 1.0006`，而 `unit_nav = 2.5247`，
  两者无关 —— **所以 unit_nav 序列没有被这个 bug 污染**，不必担心 NAV 曲线滞后一天。

### 建议修法（待排期）

```python
"total_units": round(v / unit_nav, 2) if unit_nav else None,   # nav_engine.py:226
```
改后需全量 `rebuild_portfolio_nav()` 重刷，并补一条断言：`total_units * unit_nav ≈ total_value`。

---

## 问题四：`portfolio_nav.mwr_return` —— MWR 口径已重做（滚动 1 年 Modified Dietz 年化）

> 记录时间 2026-09-15 · 发现人 data-engineer · **状态：✅ 已修复（2026-09-15 随提交落地）**
> 修复方式：全周期 IRR 对 14 年长历史病态（补 v0 后仍 +680%），已弃用
> `_solve_period_irr` 填充，改用 `_trailing_mwr_annualized` 滚动 1 年单期 Modified Dietz
> 年化**逐行**填充；原护栏与坏值清理逻辑保留。
> 结论一句话：**该字段现在可用，语义是「近 1 年资金加权收益（年化）」**；早期低市值 + 大额出入金时点仍为 NULL，下游须判空。

### 复现查询

```sql
SELECT mwr_return, COUNT(*) FROM portfolio_nav GROUP BY mwr_return;
-- 2026-09-15 重建前: -0.735121 | 3477
-- 2026-09-15 重建后: 10.444292 | 3477     ← 值变了，但「全表同值」的性质没变
```

### 需要修正的两处误判

**其一**：初判为「从 2026-08-27 起恒为死值」，实测**不是区间问题，是全表问题**——
全表 3477 行（2012-05-28 ~ 2026-09-14）永远是同一个值。

**其二**：这个值**不是固定常数，每次 `rebuild_portfolio_nav()` 都会重算并改变**。
2026-09-15 因场外净值补齐触发重建，`mwr_return` 由 **−0.735121 变为 10.444292**（即 +1044%）。
所以不要把它当成「死值」写进任何断言。

另外「每行相同」本身**不是 bug，是设计**：`nav_engine.py:237-241` 注释写得很清楚——
「全周期 MWR，写入每行便于任意行读取」，即把算出来的全周期 IRR 广播到每一行。

### 真正的 bug：IRR 结果与 TWR 差了一个数量级

| 时点 | `unit_nav`（09-14） | 累计 TWR | `mwr_return` | 折算 |
|---|---|---|---|---|
| 重建前 | 2.524669 | **+152.47%** | −0.735121 | −73.51% |
| 重建后 | 2.626177 | **+162.62%** | 10.444292 | **+1044.43%** |

同一个组合、同一段现金流，TWR 与 MWR 应同号且量级相近。
重建前是 +152% vs −73.5%（差 225pp），重建后是 +162% vs +1044%（差 882pp）——
**两次都错，且重建后更离谱**。说明 `_solve_period_irr(df, cf, div)`（`nav_engine.py:238`）
的解根本不可信，而不是「某次算错了」。
可能原因：现金流符号/日期对齐错误，或 IRR 在无实根/多根时返回了边界值。

### 影响范围（已核实）

- `mwr_return` **有下游读取**：`nav_engine.py:294` `load_portfolio_nav()` 的 SELECT 含该列
  （列清单：`date, unit_nav, total_value, net_flow, twr_cumulative, mwr_return, is_suspect`），
  会被基准对比 / 回撤 / 归因等下游消费 → **用户可见的错误数字**。
- 建议：修复前先在展示侧隐藏该字段，避免 −73.5% 被当成真实收益展示。

### 已做的处置（2026-09-15）

按「先不产生错误数字、再不展示错误数字、最后才修算法」的顺序：

1. **护栏**：`src/analysis/nav_engine.py` 新增 `_MWR_MIN = -0.99` / `_MWR_MAX = 5.0`，
   在 `_solve_period_irr()` 内用 `_guard()` 包裹所有返回路径。
   解非有限值或越界 → 返回 `None` + `logger.warning` 打印输入摘要
   （现金笔数、净流入、首末笔、分红合计、期初期末市值）。
   同时给原本静默的「无实根」分支补了 warning。
   `rebuild_portfolio_nav()` 里 `if mwr is not None` 的分支不变，
   因此返回 `None` 时各行 `mwr_return` 自然写入 `NULL`。

2. **清坏值**：`scripts/purge_bad_mwr.py`（新建，判据与 `_MWR_MIN/_MWR_MAX` 同源）。
   已执行：`UPDATE portfolio_nav SET mwr_return = NULL WHERE ... 越界`，
   **清理 3477 行**，现全表 `mwr_return IS NULL`。

3. **展示侧容错**：`get_nav_series()`（原 `load_portfolio_nav` 位置的读取函数）
   docstring 已注明 `mwr_return` 可能为 NULL/NaN，下游必须显示"暂不可用"，
   **不要**渲染成 nan / None / 0，也不要用 0 参与运算。
   ⚠️ 核实结论：全项目 grep `mwr_return` 只有 `nav_engine.py` 内部 +
   `db_schema.py` 建表 + 本文件，**当前没有任何展示层调用 `get_nav_series()`**，
   所以暂无实际渲染点需要改。未来新增消费方时请回读本条。

4. **端到端验证**：重新 `rebuild_portfolio_nav()` 后确认
   `越界坏值行数 = 0`、`mwr_return IS NULL = 3477/3477`，
   且护栏 warning 正常打印，其余字段（unit_nav 2.626177 / TWR +162.62% / is_suspect 0）不受影响。

### 为什么区间取 [-0.99, 5.0] 而不是更宽

不要用 `mwr < 10` 这类宽区间：+1044% 能被拦，但 +900% 同样会被放行，
而那依然是错的。取 +500% 作为上界，已远超任何合理组合收益，
足以把发散解挡住，又不至于误伤正常值。

### 修法（待排期，属另一个专项）

**本轮不修 IRR 算法本身。** 排查线索（护栏 warning 打印的输入摘要，2026-09-15）：

```
期间 2012-05-28~2026-09-14 (3477 天), 现金流 380 笔, 净流入 1,109,683.60,
首笔 29,000.37@2012-05-29, 末笔 -8,000.00, 分红合计 4,903.79,
期初市值 10,416.00, 期末市值 1,505,090.13   →  解出 10.444292
```

两个可疑点，供后续专项参考：

- **时间权重口径**：`_solve_period_irr` 里 `frac = (t - i) / t`（`t` 为总天数），
  折现因子是 `(1 + r) ** frac`，这把 `r` 定义成**整个持有期的收益率**而非年化。
  但字段名与注释都指向"全周期资金加权收益"，语义与用法是否对齐需先确认。
- **现金流符号**：`dep = -float(cf.get(...))`，净流入 110 万而期末市值仅 150 万、
  期初 1 万，直觉上全期收益应在数十个百分点量级，解出 +1044% 需核对
  `trade_records` 里是否存在符号或口径（如分红、转存）错记。

建议步骤：
1. 先给 `_solve_period_irr()` 补单元测试：构造已知 IRR 的现金流，断言输出。
2. 确认 `frac` 的时间口径（全期 vs 年化）与字段语义一致。
3. 抽样核对 `trade_records` 中若干笔大额的 `change_amount` 符号。
4. 修好后重刷 `portfolio_nav`，并同步更新 README / 日报口径说明。

### ✅ 已修复（2026-09-15，随提交落地）

**没有修 IRR 求解器——而是换掉了填充口径。** 根因复盘：

1. **真正的 bug：NPV 方程漏 `v0` 项**。`_solve_period_irr`（`nav_engine.py:128`）
   只把中途入金 `dep` 折现，没把期初持仓 `v0` 当 t=0 投资 → 解出 +1044%（10.444292）
   伪根，与 TWR（+162%）差 882pp。补回 `s -= v0 * (1 + r) ** 1.0` 后仍是 **+680%**——
   说明全周期 IRR 对 14 年、3477 行、现金流相对早期市值巨大的序列**本身病态**，
   不是方程还能救。补 v0 是必要正确性修复，但本函数**仅保留作诊断**，不再填充列。
2. **改用滚动 1 年单期 Modified Dietz 年化**（`_trailing_mwr_annualized`，`nav_engine.py:214`）：
   窗口短 → 现金流相对市值小 → 月末 `total_value` 失真被摊销 → 稳定可用。
   实测最近交易日连续 ~**+43%**（同数据下链式逐日 MD 爆炸到 ~+270%，故只用单期、不链式）。
   与 TWR 同号、量级合理（MWR 略低于长周期 TWR，符合「近期波动较小」直觉）。
3. **逐行填充**：`rebuild_portfolio_nav()` 改为 `mwr_list = _trailing_mwr_annualized(df, cf, div)`，
   每行填对应滚动值；早期低市值 + 大额出入金时点（窗口内 `v_begin<=0` 或分母 `<=0`）→ `None`。
4. **护栏与坏值清理保留**：`_MWR_MIN/_MWR_MAX` 区间、`_guard()`、`scripts/purge_bad_mwr.py`
   仍生效，越界解一律 `None`；`get_nav_series()` 的 docstring 已更新为「逐行滚动、仍须判空」。

**下游消费约定（更新）**：`mwr_return` 不再是全表同值，而是「截至该交易日的近 1 年资金加权
年化收益」；展示前仍须判 `None`/`NaN`，显示"暂不可用"，**不要**用 0 参与运算。

> README / 日报里若仍写着「MWR 不可信 / 暂不可用」，随本次修复可改为正常展示；
> 同时 TWR 最终口径应统一为 **+162.56%**（unit_nav 2.625635，2026-09-14），旧值 152.47% / 162.62% 作废。

---

## 问题五：TWR 序列是「历史段场内主导 + 近期全组合」的拼接口径

> 记录时间 2026-09-15 · 发现人 data-engineer · **状态：刻意保留，不修**
> 本条不是 bug，是**口径限制**。读 `portfolio_nav` / `portfolio_summary` 前必读，
> 否则会把不同口径的收益串在一起比较。
> **延伸阅读**：风险指标（vol / mdd / sharpe）同源继承该口径，且「补场外」不是修法 ——
> 见本节末 **「补充（2026-09-16）」**，读风险三列前必读。

### 背景

2026-09-15 用 `scripts/fetch_otc_fund_nav.py` 补齐了场外 13 只基金 2026-08-03 ~ 2026-09-14
的日频净值快照：**共 378 行**（12 只普通场外 347 行 + 货币型 `880013` 31 行，口径见问题六）。
此前场外基金**只有月末快照**，详见本文「场外标的清单」。

### 口径现状

| 时间区间 | 场外 13 只覆盖 | daily_return 实际由谁贡献 |
|---|---|---|
| 2012-05-28 ~ 2026-01-30 | 无 | 场内 |
| 2026-01-31 ~ 2026-07-30 | **仅月末那一天** | 月末日含场外，其余日仍只有场内 |
| 2026-08-03 ~ 2026-09-14 | **日频**（本次补齐） | 全组合（22 场内 + 13 场外） |
| 2026-09-15 起 | 日频（若采集器接入日常链路） | 全组合 |

**即：修正后的 TWR 是一段拼接序列，不是全周期一致口径。**
07-31 及之前，绝大多数交易日的收益只反映场内 22 只（约占总市值 62%）；
08-03 起才反映全组合。跨过 2026-08-03 做区间收益对比时，口径发生了切换。

### 为什么不做全历史回补

**不要**顺手把场外历史也全量回补成日频。原因：

- 场外基金的历史**份额变动不可考**。`portfolio_snapshots` 只在月末那一天有记录，
  中间的申购/赎回无从还原。
- 用「当前份额 × 历史净值」倒推历史市值，会凭空造出从未持有过的资产，
  引入的错误比现在更大。
- 现有月末快照里的 `quantity` 是当天的真实份额，只有那一天的市值是可靠的。

### 若要彻底一致，需要什么

1. 拿到场外 13 只的**完整申赎流水**（券商/基金App 导出的对账单），按日重建份额；
2. 或用 `trade_records` 反推（当前表内是否覆盖场外申赎需另行核实）；
3. 之后才能做全历史日频回补并重刷 `portfolio_nav`。

**在此之前，任何跨 2026-08-03 的收益对比都必须注明口径切换。**

### 补充（2026-09-16）：**风险指标同源继承该口径**，且「补场外」不是修法

> 记录人 risk-manager · **只读副本上实测**，未写库。本节是对上文的必要外延：
> 上文只说了收益（TWR）受影响，**实际波动率 / 最大回撤 / 夏普同样受影响**，因为它们的输入就是 `daily_return`。

#### 传导链路（源码行号）

`src/analysis/portfolio_risk.py:52-80`：

- `:64-68` `returns = daily_return / 100` → 供 `calculate_all()` 算 vol / sharpe；
- `:55-58` `full_prices = np.cumprod(1 + daily_return)` → 供 drawdown 分窗口（60d/1Y/ALL）。

即 **风险侧三列全部由 `portfolio_summary.daily_return` 单向决定**，不读 `total_value`。
所以上文「2026-01-31 ~ 07-30 绝大多数交易日只反映场内 22 只（约 62% 市值）」的系统性低估，
**同时传导为波动率与回撤的低估**。当前 P1-3 交付物里的 `sharpe_60 = +0.7291`、`mdd_60 = 6.72%`、
`mdd_all = 9.83%` 都建于该口径（`mdd_all 9.83%` 与 v2.5 提交 `1567d5a` 修正后的数值同源）。

> **口径依赖注记**：上述风险数字基于**口径 A**（场外 13 只 ~38.5% 仓位的区间收益不计入日序列）。
> 口径变更则 vol / mdd / sharpe **必须重判**。**已实测**（下节四条序列）：
> 窗口 `sharpe_60` 在 A / B / C' 三种口径下为 **+0.7291 / −0.8511 / −1.1346** ——
> **符号不稳健**，故「夏普转正」只能表述为「在现库口径下转正」，不可作为结论外推。

#### 为什么不能靠「把场外补进日序列」来修（方案 B 实测）

2026-01-31 ~ 2026-07-30 场外**只有月末行、无任何日频源**（见上表），且本文档明确记载
不可事后全量回补（份额变动不可考）。因此在这一段，「补场外」在数学上等于
**把月末快照的整月收益一次性记到月末那一天**，而不是还原日频路径。

只读副本 A/B 实测（窗口 2026-01-31 ~ 2026-09-14，150 天，公式同
`scripts/recompute_summary_window.py:112-135`，rf = 2.5%）：

| 指标 | A（现库口径） | B（补场外） | Δ |
|---|---|---|---|
| 窗口累计 | −0.6295% | −10.5488% | −9.92pp |
| 2026-07-31 单日 | **+0.8182%** | **−8.6555%** | −9.47pp |
| vol_all | 18.91% | 22.20% | +3.29pp |
| mdd_all | 9.83% | 15.69% | +5.86pp |
| vol_60 | 22.08% | 28.35% | +6.26pp |
| mdd_60 | 6.72% | 14.50% | +7.78pp |
| **sharpe_60** | **+0.7291** | **−0.8511** | **符号翻转** |

把这 150 天的 Δ 逐日拆开，**只有 12 天有差，且分成两个高度规整的簇**：

| 簇 | 变化日 | 星期 | Δ 合计 | 占比 |
|---|---|---|---|---|
| ≤ 2026-07-31（日历月末快照行） | 02-28、03-31、04-30、05-31、06-15、07-31 | Sat / Tue / Thu / **Sun** / Mon / Fri | −9.9856pp | 98.97% |
| 　其中 07-31 单日 | 07-31 | Fri | −9.4737pp | 93.90% |
| > 2026-07-31 | 08-07、08-14、08-21、08-28、09-04、09-11 | **连续 6 个周五** | −0.1040pp | 1.03% |
| **合计** | 12 / 150 天 | | **−10.0896pp** | 100% |

两点值得注意：

1. **02-28 是周六、05-31 是周日** —— 这些是**日历月末快照行**，不是交易日，
   且与另一条独立诊断线列出的「月末快照仓量异常」清单重合。
   即 B 的 Δ 由**异常快照行**驱动，不是平滑的覆盖补齐。
2. **08-03 之后只在 6 个周五有差** —— 机制已查明，见下。

#### 机制定论：B 与 A 的差 = 「最近一行兜底」vs「精确匹配」，根因是 `027293` 只按周落行

A 与 B 的定义差异：

| 口径 | 取 `prev` 的规则 | 取不到时的行为 |
|---|---|---|
| **A** | 精确匹配 `snapshot.date == prev_dt` | **该标的当日不入篮**（丢弃） |
| **B** | 取 `date <= prev_dt` 的**最近一行** | **用陈旧价兜底**（最多滞后 5 个交易日） |

实测 `portfolio_snapshots` 2026-08-01 ~ 09-16 逐 code 落行情况：

| 代码 | 落行日数 | 是否日频 |
|---|---|---|
| 其余 12 只场外 | 32 | ✅ 日频 |
| **`027293`**（东吴产业趋势混合A） | **6** | ❌ **只在周五**（08-07/14/21/28、09-04/11） |

推断完全吻合实测的 6 个周五：

- **周五**：`027293` 当日有行（curr 可取），但其 `prev_dt`（周四）**无行**
  ⇒ A 丢弃该标的、B 用**上周五的旧价**兜底 ⇒ **两口径产生差**；
- **周一至周四**：`027293` 当日无行 ⇒ A、B **都取不到 curr**，同样不入篮 ⇒ **无差**。

`12 × 32 + 6 = 390` 行，与采集器报的「390 行」逐位吻合，这条推断可复核。

**因此：B 在 2026-08-03 后那 1.03% 的增量不是「真实场外日频补齐」，
而是 `027293` 的陈旧价兜底 —— 与 07-31 那 93.90% 同属计价伪影。**

> **(a)「真实场外日频补齐」实为 0**（此前本节曾按「≈1%」登记，据此更正）。
> B 的 Δ 全部由计价伪影构成：**月末一次性记账（98.97%）+ 周频陈旧价兜底（1.03%）**。

> **附带发现（需采集侧处置）**：`027293` 在 2026-08-03 之后**只按周更新**，
> 而同批其余 12 只均为日频。这是**独立的覆盖缺陷**，与本文档问题五的口径无关，
> 但会让 `027293` 的日频收益长期缺失，并使任何「最近一行兜底」类算法在此标的上产生陈旧价。
> 建议采集侧核查该代码的落行原因（是源本身周更，还是采集器跳过）。
> 另：A 口径在 6 个周五会**丢弃** `027293`（不入篮），这也是 A 侧的一个小缺陷。

**关键判定**：这条路径会把 v2.4 提交 `942a527`（用 corrected `daily_return` 替代
`total_value.pct_change`，波动率 262% → 17%）与 v2.5 提交 `1567d5a`（消除场外基金月度入账
导致的回撤虚高，31.77% → 9.83%）**已经移除的效应，重新塞回 `daily_return`**。
即 B 用「水平修正」换来「伪尖峰污染」，是**交换**而非改善。

#### 三者的正确刻画

| | 水平（累计 / TWR） | 日序列（vol / mdd / sharpe 依赖） |
|---|---|---|
| **A** 现库 | 系统性偏低（丢掉 ~38.5% 仓位的收益） | **干净**（不含伪造尖峰） |
| **B** 补场外 | 接近真实 | **被伪尖峰污染**（07-31 +0.82% → −8.66%） |
| **C'** 区间摊销 | 接近真实（摊平引入 −0.09pp 漂移） | 形态干净，但 **mdd / sharpe 比 B 更差**（见下） |

**A 丢水平、保日序列；B 补水平、毁日序列；C' 补水平、保形态但压深回撤。三者都不是「真」。**

#### 四条序列的风险三列实测（2026-09-16，含 C'）

由数据诊断线产出四条 `daily_return` 序列、本节作者独立复算指标（公式同上，只读）：

| 序列 | vol_all | sharpe_all | mdd_all | vol_60 | sharpe_60 | mdd_60 | 窗口累计 |
|---|---|---|---|---|---|---|---|
| **A** 现库 | 18.91% | −0.0943 | 9.83% | 22.08% | **+0.7291** | 6.72% | −0.6295% |
| A' / A_narrow | 18.91% | −0.0943 | 9.83% | 22.08% | +0.7291 | 6.72% | −0.6295% |
| **B** 补场外 | 22.20% | −0.8440 | 15.69% | 28.35% | **−0.8511** | 14.50% | −10.5488% |
| **C'** 区间摊销 | 19.36% | −1.0085 | **16.22%** | 22.90% | **−1.1346** | 14.47% | −10.6409% |

> A' 与 A_narrow **逐位等于 A**（0 天有差）：`027293` 在 `portfolio_snapshots` 只有 8 行且
> **不存在任何「相邻两天都有行」的日期对**，故它在 A 口径下从不入篮，修正其占位价对 A 是空操作。
> 四列表实际只有三条不同序列。

#### C'（区间摊销）：达到目的的 1 项 + 需要解释的 2 项

做法：把 B 的断层涨跌按该段交易日数**几何摊平**回区间内（12 个断层段）。

- **达到目的**：`vol_60` 22.90% ≈ A 的 22.08%，不再有 07-31 那种伪尖峰，日序列形态干净。
- **需要解释**：`sharpe_60` **−1.1346**（B 为 −0.8511）、`mdd_all` **16.22%**（B 为 15.69%），
  即 C' 在这两栏上**比 B 更保守**。这不是 C' 的缺陷，而是两件不同的事：

**其一：负均值区间里「降波动」对夏普是减分项（数学必然后果，非口径优劣）。**
摊平保均值、抹平方差 ⇒ 波动下降；而窗口均值为**负**，
Sharpe = (mean·252 − rf) / vol 的分子不变、分母变小 ⇒ **夏普机械变差**。
所以**不能**用「C' 夏普更差」推断 C' 更差——它只说明低波动在亏损区间里帮不上夏普。

**其二：回撤更深有两种读法，取决于无观测段的路径假设（必须显式声明）。**

| 读法 | 含义 | 成立条件 |
|---|---|---|
| **(i) C' 更忠实**（本文倾向） | 摊平 = 对月内路径取**最大熵假设**（均匀下行、无中途反弹）；B 把整月跌幅压进一天，反而**低估**回撤 | 月内路径近似单调 |
| (ii) C' 高估 | 若月内实际有反弹，摊平会造出「假连续下坡」，深层回撤不真实 | 月内确有明显反弹 |

无观测 ⇒ **两种读法都不能排除**，但 (i) 是更保守、更适合风险度量的一侧。
按 (i)，C' 的 16.22% 不是「C' 的代价」，而是 **A 与 B 都藏起来的那部分回撤被显性化**。

**共同结论（与读法无关）**：三条序列在**夏普符号上一致地把 A 的「转正」推翻**。

> **无论采纳 B 还是 C'，窗口 `sharpe_60` 都是负的（−0.8511 / −1.1346）。**
> 所以「夏普转正」**不是稳健结论**，而是**口径 A 的产物**。这是本条的最终判定。

补充三点界定：

- **A' / A_narrow 是空操作**（见上），四列表实际只有三条不同序列。
- **C' 的改动面远大于 B**：138 / 150 天与 A 不同（B 只动 12 天）。改动面越大、可归因性越差，
  选口径时应把这一点计入。
- **摊平不严格保水平**：C' 窗口累计 −10.6409% vs B 的 −10.5488%，漂移 0.09pp。
  若要求水平严格一致，应改用几何摊平 `(1+r)^(1/k) − 1` 而非 `r/k`。
- **两类 vol 不可混**：`etf_features.vol_20d / vol_60d` 源 `etf_price_history`（逐标的），
  实测 A/B 副本下该表 34,400 行 md5 逐字节相同；本节传导链只吃
  `portfolio_summary.daily_return`（组合级聚合）。
- **全历史头条 `max_drawdown` 不受影响**：A = B = 51.30%（峰 2021-02-10 → 谷 2024-02-05），
  口径差异只体现在 2026 年这一段窗口。

---

## 问题六：`880013` 净值取数 —— 已解决，按「货币基金份额增长」口径计价

> 记录时间 2026-09-15 · 发现人 / 处置人 data-engineer
> **状态：已解决（有可用源）。`NO_NAV_SOURCE_CODES` 标记未添加 —— 因为源是通的。**

### 背景

场外 13 只里，`880013` 是唯一一只长期停在 **2026-07-31**（45 天未更新）的标的。
此前评估结论是「无净值源，跳过」，陈旧估值金额 9,443.09 元。

### 为什么此前判定取不到

`880013` = **招商资管智远天添利货币**，类型 **货币型-普通货币**
（用 `ak.fund_name_em()` 可查到，属公募目录内）。
它是**券商资管现金管理类产品**，不是标准开放式净值型基金，所以：

| 试过的接口 | 结果 |
|---|---|
| `ak.fund_open_fund_info_em(symbol="880013")` | ❌ `JSEvalException: Data_netWorthTrend is not defined` —— 页面里根本没有单位净值曲线 |
| `ak.fund_money_fund_info_em(symbol="880013")` | ❌ `ValueError: Expected axis has 14 elements, new values have 13` —— **不是没数据**，是 akshare 把返回列名硬编码成 14 个，货币型接口只回 13 列，赋值时对不上 |
| `ak.fund_name_em()` | ✅ 能查到名称与类型，确认是货币型 |
| 直连 `api.fund.eastmoney.com/f10/lsjz`（`FSRQ`/`DWJZ`/`LJJZ`） | ✅ **成功**，`TotalCount=1462`，覆盖 2026-04-08 ~ 2026-09-14，含 每万份收益 / 7 日年化（≈0.70%） |

**根因**：akshare 的封装层问题，不是数据源缺失。绕开 akshare 直连东方财富原始
JSON 接口即可拿到数据。

### 当前按什么口径计价

货币型基金**单位净值恒为 1.0**，收益不体现在净值上，而体现在**份额增长**：

```
每万份收益 X 元  ⇒  份额 ×= (1 + X / 10000)
market_value = 份额 × 1.0
成本总额固定     = 份额₀ × 成本单价（成本单价不随收益变）
pnl            = market_value − 成本总额
```

即：价格列恒 `1.0`，市值增长完全由 `quantity` 列承载。
这与普通场外基金（净值变、份额不变）**是两套相反的口径**，
做横向汇总或写校验断言时不要混用。

### 落地结果

- 写入 **31 行**（2026-08-03 ~ 2026-09-14，只在 `portfolio_snapshots` 已有交易日写入，
  不凭空造交易日）。
- 份额 9,443.09 → **9,451.16**，区间累计收益 **8.07 元**（约 0.085%，与 7 日年化 0.70% 量级一致）。
- 零改写校验（对照写入前备份）：36,108 → 36,139 行，**旧行被修改 0，旧行丢失 0**。
- 陈旧估值金额：**9,443.09 → 0.00**（13 只场外全部更新到 09-11 ~ 09-14）。
- 占比：9,451.16 / 1,514,541.29 = **0.624%**（2026-09-14 组合总市值口径）。

### 代码位置

`scripts/fetch_otc_fund_nav.py`：

- `MONEY_FUND_CODES = {"880013"}` / `MONEY_FUND_NAV = 1.0`
- `fetch_money_fund_yield(code, max_pages)` —— 直连东方财富，绕开 akshare 14 列硬编码
- `build_money_fund_rows(...)` —— 按上述份额增长口径生成行

`run_otc_nav()` 与 CLI `main()` **两条路径都已接上货币型分支**（此前只接了一条，
导致 dry-run 仍失败，已修）。

日常链路：`run_analysis.py:79` `run_stage0_otc_nav()` 在阶段一之前调用 `run_otc_nav()`，
所以 `880013` 会随每日跑批自动续更，不是一次性手工补数。
（`run_analysis.py:94` 原注释「880013 无公开净值源，按设计跳过」已同步更正。）

### 兜底约定（重要）

若东方财富直连接口日后再次失效，采集器会 `logger.warning` 并**跳过该代码**，
**绝不硬编码净值、也绝不沿用上一次的旧值**。届时：

- 该标的按**成本计价**；
- 日报 / 展示必须显式标注「**净值不可用，按成本计价**」，不能静默复用 45 天前的数字。

是否需要为此在 `config/settings.py` 的 `SNAPSHOT_STALE_DAYS_OVERRIDE` 旁新增
`NO_NAV_SOURCE_CODES`（语义「无净值源，按现金等价物处理，豁免陈旧告警」）——
**当前不需要**：源是通的，`880013` 已能日频更新到 09-14，天然不触发陈旧告警。
等真正出现取不到的那天再加，避免把一个活着的源提前标成死的。

> 展示层怎么处理这只标的（价格恒 1.0 会被当成"没涨"），见下一节
> **「附：货币型场外基金的展示口径」**。

---

## 附：货币型场外基金的展示口径

> 记录时间 2026-09-15 · **只约束展示层，不改代码、不改数据口径**
> 适用范围：`880013`（招商资管智远天添利，货币型-普通货币），
> 以及将来任何新增进 `MONEY_FUND_CODES` 的货币型标的。

### 数据特征

| 字段 | 普通场外 | 货币型（880013） |
|---|---|---|
| `current_price` | 净值，逐日变动 | **恒为 1.0** |
| `quantity` | 不变（无申赎时） | **单调增长**（收益转份额） |
| `market_value` | 份额 × 净值 | 份额 × 1.0，**数值上等于 quantity** |
| 收益体现 | 价格上涨 | **份额增长** |

即：货币基金不是"不涨"，是**涨在份额上而不是价格上**。

### 展示要求（必须）

1. **必须标注**「**货币基金 · 净值 1.000 · 收益计入份额**」。
2. **不要只渲染 `current_price`** —— 它恒为 `1.000`，用户看到会以为这只标的一个月没动。
3. **收益/涨跌幅取 `pnl_rate`**（= `pnl` / 固定成本总额），**不要**用
   `(current_price − cost_price) / cost_price` —— 后者对货币型恒为 0，
   会把每天的小额正收益显示成"平盘"。
4. 若某组件按**价格涨跌幅**排序 / 着色 / 打标签，880013 会恒为 0%，
   永远落入"平盘"分组，既不涨也不跌 —— **实际它是每天小幅正收益**。
   这类组件应对货币型走市值变动（`market_value` 日差）或直接跳过。

### 两个数值陷阱

- **市值与份额数值相同**：09-14 的 `market_value = 9,451.16 元`、`quantity = 9,451.16 份`，
  两者数字一样但单位不同（元 vs 份）。别因为"看起来相等"就互相代用或当成校验通过。
- **别写「quantity 恒定」的断言**：普通场外无申赎时份额不变，这条断言成立；
  但对货币型**必然误报**——它的份额每天增。校验脚本需按 `MONEY_FUND_CODES` 分支处理。
  而 `market_value == quantity × current_price` 这条**仍然成立**（乘以 1.0），不要据此认为口径一致。

### 源失效时的兜底文案

若东方财富直连源失效、采集器跳过该标的（按成本计价），展示文案改为：

> **净值不可用，按成本计价**

**不要**静默沿用上一次的旧值——那会让它看起来还在正常更新。

---

## 问题七：`etf_predictions` 表已封存 —— 主动封存，不要再补

> 记录时间 2026-09-15 · 记录人 data-engineer
> **状态：✅ 主动封存。本表自 2026-08-19 起不再产出新行；表内 5 个 model 全部已 VETO 下线，
> 不可用于任何决策或展示。**

### ⚠️ 先区分：这是「主动封存」，不是「数据待补」

本轮同时处理了两类停在 8 月的表，**性质完全相反，不要混为一谈**：

| 表 | 停在 8 月的原因 | 处置 | 现状 |
|---|---|---|---|
| `portfolio_snapshots` / `portfolio_summary` / `portfolio_nav` | **被动停更**（管线断了 / 场外无日频源） | **已补齐并接入日常管线** | 已到 **2026-09-14** |
| `etf_predictions` | **主动封存**（5 个模型全部 VETO，产出即错） | **不再补，也不要补** | 停在 2026-08-19，保持原样 |

两张情况都跟「2026-08-19 前后」这个日期沾边，极易被误判成同一件事。
**看到 `etf_predictions` 停在 08-19，正确反应是「这是对的」，不是「又要补数据」。**

### 表内现状（2026-09-15 实测，只读）

```
总行数 286，只有 2 个日期：2026-08-18（66 行）、2026-08-19（220 行）
```

| model | 行数 | 日期 | forward_window | 预测目标 | 状态 |
|---|---|---|---|---|---|
| `tier0_signal_ensemble` | 66 | 2026-08-18 | 5 / 20 / 60（各 22） | 方向 | ❌ VETO |
| `lgb` | 66 | 2026-08-19 | 5 / 20 / 60（各 22） | 方向 | ❌ VETO |
| `ridge` | 66 | 2026-08-19 | 5 / 20 / 60（各 22） | 方向 | ❌ VETO |
| `risk_lgb` | 44 | 2026-08-19 | 20 / 60（各 22） | 波动率 | ❌ VETO |
| `risk_lgb_v2` | 44 | 2026-08-19 | 20 / 60（各 22） | 波动率 | ❌ VETO |

**5 个 model 全部 VETO，表内没有任何一行可用于决策或展示。**

### 封存依据（已定稿的结论，勿再翻案）

1. **方向模型（Tier0 / lgb / ridge）**：walk-forward 方向命中率 **46–50%**，与抛硬币无异；
   Tier1 样本外截面 **IC = 0.006**，远低于否决线 `VETO_IC = 0.02`
   （`src/analysis/predictor/models.py:43`、判据在 `models.py:191-200` 的 `_verdict()`）。
   同源结论亦见 `src/analysis/etf_position.py:6`：
   「项目已用 walk-forward 证明 ETF 短期方向不可测（Tier1 VETO, IC<0.02）」。
2. **波动率模型（risk_lgb / risk_lgb_v2）**：修正标签（窗口 `[t+1..t+n]`，消除未来函数）后，
   样本外截面 Spearman IC **0.613** 未跑赢零成本的 `vol_20d` 基线 **0.743**，
   ΔIC 的 **HAC t = −4.51**，3 窗口 × 2 模型 **6/6 全部 VETO**（BH-FDR q < 0.0001）。
   原文见 `models.py:363-368` 的 `_DEPRECATED_RISK_MSG`。

   > **面板范围口径注（2026-09-16 补 · quant-analyst）**：以上「6/6 VETO」的**面板范围**此前
   > 未显式记录。同日（2026-09-16）在修正后特征上重跑门禁时实测：
   > ① 面板含 2012–2017 的 4091 行，而 `etf_price_history` 自 **2018-01-02** 才有数据，
   > 这些行 OHLC/volume 派生特征结构性全 NULL，会触发 P1-6 缺失率护栏 ⇒ 门禁**跑不到第一折**；
   > 故本轮把面板收窄到 **`>=2018-01-02`（30,309 行）** 方可运行（见 task #84）。
   > ② 在该范围下结果为 **5/6 VETO**（仅 `w=20 ridge` PASS）。**与上表 6/6 的差异来自「面板范围 + 数据延伸」，
   > 与 volume 手/股修复无关**——同面板重建污染前后逐折 verdict 逐位相同。
   > ③ `walkforward_splits` 的 OOS **只覆盖到 2025-11-06**，其后约 1/6 数据（含整个 2026）
   > **从未进入 OOS**（见 task #83）⇒ 本结论只基于 **2025-11 之前**的数据，
   > 且任何 2026 年的数据污染**结构性无法在 OOS 里体现**。
   > **引用/对照本结论时请连带面板范围一并引用，勿跨范围直接比较「6/6」与「5/6」。**

替代口径：波动率直接读 `etf_features.vol_20d / vol_60d`，
见 `tabs/tab16_risk_outlook.py`、`src/utils/risk_report.py`、`src/analysis/rebalance_engine.py:664`。

### 代码护栏：别顺手把它跑起来

`src/analysis/predictor/models.py:372-379`：

```python
def _guard_deprecated(name: str, allow: bool) -> None:
    """已下线函数的运行时护栏：防止有人顺手 import 后重写 etf_predictions。"""
    if allow:
        return
    raise NotImplementedError(
        f"{name} 已停用（能力已移除），禁止调用。{_DEPRECATED_RISK_MSG}"
        " 确需重跑审计时显式传 allow_deprecated=True。"
    )
```

`run_risk_prediction()`（:382）与 `run_risk_predict_latest()`（:442）首行即调用
`_guard_deprecated(...)`，**默认 `allow_deprecated=False` → 直接抛 `NotImplementedError`**。
只有显式传 `allow_deprecated=True` 才会执行（会重写已封存的 `risk_lgb` 44 行，仅用于重跑审计）。

### 为什么它不会自己长出新行

`run_tier0()`（`tier0.py:102`）与 `run_tier1()`（`models.py:230`）**在生产链路中零调用方**——
全项目 grep 只有 `models.py:591` 的手动入口与 `tests/test_predictor_models.py` 引用。
所以 08-19 之后没有新行是**预期行为**，不是管线故障。

### 使用约定

- **不要**读本表做任何择时、调仓、加仓减仓判断。
- **不要**在日报 / 看板 / Tab 里展示本表的 `direction` / `score` / `confidence`。
- **不要**为了「把数据补到 09-14」而重跑 `run_tier0` / `run_tier1` / `run_risk_prediction`
  —— 补出来的行是已知错误的产出。
- 确需审计重跑时，显式传 `allow_deprecated=True` 并**先备份数据库**。
- 历史 286 行**保留不删**，作为「当时做过什么、为什么否决」的留档
  （`scripts/verify_risk_downgrade.py` 会校验 `risk_lgb` 44 行仍在）。

---

---

## 问题八：`etf_technical` 是**三套口径**混在一张表里（含「日期与 K 线末日错位一天」）

> 记录时间 2026-09-15 · 发现人 data-engineer
> **状态：仅记录，未修。** 本条是给「159732 观察名单采集」做的副产品核查，
> 结论适用于**全部标的**，不只对 159732。

### 三套口径分别是什么

| # | 口径 | 写入方 | 价格来源 | 典型特征 |
|---|---|---|---|---|
| ① | backfill 口径 | `scripts/backfill/backfill_full_history.py:498` `rebuild_etf_technical()` | `portfolio_snapshots.current_price` | `trend` ∈ {强势上涨, 温和上涨, **下跌**, 震荡整理}；`kdj_signal` 只有 金叉/死叉 |
| ② | 日频 TechnicalAnalyzer 口径 | `src/analysis/portfolio.py:234` `save_technical_indicators()` | `ds_manager.get_kline(count=40)` | `trend` 含 **强势下跌 / 温和下跌**；`kdj_signal` 含 中性 |
| ③ | 观察名单口径（本次新增） | `scripts/fetch_watchlist_quotes.py` | `etf_price_history` | 同 ② 的算法，但 K 线末日 = 行日期（见下） |

实测各口径占比（2026-09-15）：

| 标的 | 总行数 | ① backfill | ② TechnicalAnalyzer | 切换点 |
|---|---|---|---|---|
| 512010 | 2676 | 2649 | 27 | 2026-05-18 |
| 512810 | 2421 | 2388 | 33 | 2026-05-22 |
| 159732 | 1206 | 1179 | 27 | 2026-07-16 |

**即：把 `etf_technical` 当一条连续时间序列来读，是错的。** 跨切换点比较
`rsi_value` / `bollinger_position` 时，算法和输入价格都换过。

### 关键缺陷：日期与 K 线末日错位一个交易日

`portfolio.py:234` 用 `self.today` 当行日期，但 K 线取的是**当时能拿到的最近 40 根**。
跑批时点若早于行情源更新，K 线末日就是**上一交易日**，于是：

> **行上写的日期是 D，指标实际算的是截至 D−1 的 K 线。**

实测（159732，用库内行情按各日 K 线末日重算 RSI，与存量行逐一对位）：

| K 线末日 | 重算 RSI | 存到哪个日期 | 错位 |
|---|---|---|---|
| 2026-07-24 | 33.19 | **07-27** | +1 交易日 |
| 2026-07-28 | 34.04 | **07-29** | +1 交易日（07-28 那行是同日，不一致） |
| 2026-07-29 | 25.14 | **07-30** | +1 交易日 |
| 2026-07-30 | 25.85 | **07-31** | +1 交易日 |
| 2026-07-31 | 33.17 | （清仓后停更，无 08-03 行） | — |

且**不是每天都错**：07-28 那行是同日，07-29 又是滞后一天——
取决于当天跑批时点行情源是否已更新。所以这是**间歇性**错位，不是固定偏移，
无法用一个统一 offset 校正。

### 为什么本次的新行不沿用这个错位

`fetch_watchlist_quotes.py` 取「K 线末日 = 行日期」，即**行上写 D 就是算到 D**。
这是正确语义，但意味着 159732 的序列在 2026-08-03 处从「滞后一天」切成「同日」。
选择正确口径而不是迁就存量 bug —— 若要严格连续，得把三套口径全量重刷（见下）。

### 影响

- 技术面展示（Tab / 日报）拿 `etf_technical` 画 RSI / KDJ 曲线时，
  最近若干日的点比真实晚一天；遇到大涨大跌日会看出"指标慢半拍"。
- 任何用 `etf_technical` 做的回测 / 信号统计，日期与指标不对齐 = 变相引入未来函数
  的**反面**（信息滞后），会低估信号效果。

### 修法（待排期）

1. 先统一口径：让日频写入也走 `etf_price_history`（已验证与 `ds_manager.get_kline`
   价格**逐字段完全一致**，40 天重叠期 0 处不一致），并把行日期改为
   **K 线末日**而不是 `self.today`；
2. 再用统一算法全量 `rebuild_etf_technical()` 重刷历史，消除 ①/② 算法差异；
3. 补断言：`etf_technical.date` == 该行指标所用 K 线的最后一根日期。

**在此之前，不要把 `etf_technical` 当连续序列跨 2026-05-18 前后比较。**

---

## 优先级判定（补充）

| 问题 | 严重度 | 紧急度 | 理由 |
|------|--------|--------|------|
| 六 · `880013` 无净值源 | ~~中~~ | ~~中~~ | **已解决**（2026-09-15）。有可用源，按货币型份额增长口径日频写入；陈旧金额归零。仅余「源失效时的兜底口径」需遵守。 |
| 七 · `etf_predictions` 封存 | 低（已处置） | 低 | 5 个 model 全部 VETO，产出即错。已加 `_guard_deprecated` 运行时护栏 + 生产链路零调用方。**风险在于被误当成「待补数据」而重新跑起来**，故在此显式标注。 |
| 八 · `etf_technical` 三套口径混用 | 中 | 中 | ①/② 两套算法 + 输入价格都不同，日期还比 K 线末日**晚一天**（间歇性，无法用统一 offset 校）。影响所有技术面展示与信号统计。修要全量重刷，属独立专项。 |
| 九 · `excel_report` 中文 fallback | ~~中~~ → **低** | 低 | 2026-09-15 复核：风险低于此前判断，**暂不改动**。详见问题九。 |
| 十 · push2his 阻尼式拒绝 | 低（已缓解） | 低 | 「代理墙」误判已纠正；真正的代码缺陷（`except` 类型写错、`fetch_etf_fund_flow` 无重试）**已修**。服务端阻尼**无法从客户端消除，已接受**；三层兜底链（逐只 / 批量 / K线估算）**任何一层都不要删**。 |
| 十一 · `portfolio_snapshots` 复制行 | 中 | 中 | `2026-06-15 ~ 06-29` 的 10 只场外标的为 fill-forward 复制行（基期实为 `06-12`），该段场外市值被系统性低估、`portfolio_summary.total_value` 同步偏低，并污染相关性 / 波动率。**既有三道守卫（跨期 >12 天 / 尖峰 >0.30 / 零方差）对本段无效** —— 复制行内部 `gap = 1` 从 12 天闸门下方通过，`06-30` 假跳变 `|log_ret| = 0.1698 < 0.30`（实测 `base` 档对这 4 根**一根都没拦下**）。2026-09-16 落地 **两档判据**（`COPY_RUN_MIN_LEN=5` / `COPY_BREADTH_MIN_N=3` / `横截面占比阈值=50%（硬编码 c*2<n）`，行 key = `(current_price, round(market_value, 2))`）：**Tier1 段长**（`L>=5` 的恒 key 连续段**含段首**整体判非观测，不加 `is_otc_fund` gate）+ **Tier2 横截面广度**（仅场外篮子内，某日 `n>=3` 且同值占比 `>=50%` ⇒ 当日这些行判非观测）；命中者一律**非观测、基期回退到最近真值观测**。实测（口径：`days=60`（**行数**）窗口、阈值 `|ρ|>0.8`、入矩阵 33 只、`snapshot_date=2026-09-15`）：零收益观测 **107 → 6**（**残留的 6 条恰为下述那 6 条真值平价日** —— 守卫抹掉的是 101 条伪零、**没有**把真零一起抹掉）、`average_correlation` **0.3480 → 0.4111**、`diversification_score` **→ 0.5889**、`high_correlation_pairs` **76 → 101**（对窗口/阈值敏感，区间 `99~107`，见下）。**但伪行与那根假跳变仍留在库里 —— 守卫只让它们进不了相关矩阵，不是根治**；且**写入方未断源** —— `08-03~09-14` **回填段内真陈旧 0 处**（实测 `190` 个可比点 100% 对齐当日净值），唯一证据是 `09-15` 的**一次连续 id 写入块**（`38408~38419`，12 行：11 只拿的是官方 `09-14` 净值、`880013` 为占位价）。**注意 `09-15` 只有 1 天样本 ⇒ 不能定论是「结构性滞后」还是「单日异常」**，待下一交易日验证（判据见 §三）。**关于代价（2026-09-16，两档口径）**：按**单名条件**「相邻两行同价」做全历史扫描共命中 **117 行** = 非观测 **111 行**（`06-17~06-29` 恒定价 99 行 + `09-15` 陈旧价行 12 行）void 正确 **+ 真实平价观测 6 行被误 void** —— `100032` 的 `08-05`/`08-21`/`09-01`、`001194`/`001407`/`002152` 的 `2026-09-09`，官方净值均与其上一日**逐位相同**。**这 6 条被误 void 正是把判据从单名条件改成两档的直接理由**：两档对它们全不命中（段长 `<=2`；横截面占比 `1/12~1/13`、`3/12 = 25%`，均远低于 `50%` —— **分母 `12/13` 之别是窗口结构所致、非笔误**，见 §六）⇒ **两档下「真实观测被误 void」这项代价 = 0**。两档自身的 void 数 = **111**（`Tier1` **100** = 10 标的 × 10 日期 + `Tier2` **11** = 11 标的 × `09-15`，生产副本实跑与我的独立复现一致）见 §六；**它与单名条件的 `111` 恰好同值但构成完全不同 ⇒ 引用时必须带口径。** |

---

## 问题九：`excel_report.py` 全英文键无中文 fallback —— 复核后降级，暂不改

### 原判定
交接评估列为「与 P0-6 同类风险」：`src/report/excel_report.py` 有 57 处英文键取值点
（去重 34 个键），**0 处中文 fallback**，建议补上。

### 2026-09-15 复核结论：风险显著低于原判断，暂不改动

关键在于**数据源性质不同**，这一点原评估没有区分：

| | P0-6 出问题的地方 | excel_report |
|---|---|---|
| 数据来源 | `smart_report.py` **解析 Markdown 的产出** | **直接读 DB 表**（`portfolio_summary` / `etf_technical` / `portfolio_snapshots`） |
| 键名从哪来 | 正则 `### \d+\.\s+\[(高\|中\|低)\]` 抓出的**中文单字** | **DB 列名**，`total_value` / `sharpe_ratio` / `max_drawdown` … |
| 会变吗 | 会——改模板格式就会变 | 不会——列名是 schema 契约，改列要动 `db_schema.py` |

P0-6 的中英文键冲突是**解析产物 vs 展示表**的口径差；`excel_report` 的键名直接来自
DB 列名，不存在"上游某天突然改成中文键"的路径。实测文件内 0 个中文键取值点，
与 DB 列名完全一致，当前无实际故障。

### 为什么不顺手改掉
改 57 处取值点是纯机械替换，会把"当前正确"的代码批量改一遍，引入回归风险，
却换不来任何当下的正确性收益。**没有故障的防御性改动，性价比不成立。**

### 什么情况下必须回来改（触发条件）
若 `excel_report` 的数据入口从「直接读 DB」变为「消费上游解析产物」
（例如改为接收 `smart_report` 产出的 dict，或接第三方导出），
则键名不再由 schema 保证，**此时必须补中文 fallback**，并复用
`src/utils/enhanced_report.py` 的 `_pick()` / `_norm_priority()` 那套别名机制，不要另写一套。

### 若真要改，正确姿势
不要改 57 处。在 `_load_all_data()`（`excel_report.py:83`，统一数据入口）返回前
做一次键名归一即可——一处改动覆盖全部下游。

---

## 问题十：push2his 会阻尼式拒绝 —— **不是代理问题**（2026-09-15 实测定论）

> 注：本文档曾有两个「问题八」（`etf_technical` 三套口径、`excel_report` 全英文键），
> 已统一编号：`etf_technical` 保持问题八、`excel_report` 顺延为问题九，本节为问题十。

### 此前误判
早期记录（见 `docs/自动化采集完善方案.md` 的 D3 项）写的是
「主源 EM 被代理墙挡：`push2his.eastmoney.com` 返回 ProxyError」。
**这个结论是错的**，按它去查代理配置会查不到东西。

### 实测真相（2026-09-15）

| 检查项 | 结果 |
|---|---|
| 模块是否已绕代理 | **是**。`fund_flow.py:18-19` 弹出全部 `*_proxy` 环境变量；`:62-66` 把 `requests.Session.__init__` 改成默认 `trust_env=False`；`_urllib_get_json` 用 `ProxyHandler({})`。实测 `requests.Session().trust_env == False` |
| 绕代理后能否连通 | **能**。`curl --noproxy '*'` 直连 `push2his` 返回 200；`check_push2his_available()` 返回 `True` |
| 那为什么还失败 | push2his 对**高频直连**做阻尼：约 20~40% 的请求被掐断或返回 `data: null` |
| 失败形态 | 两种：① TLS/HTTP 层 `RemoteDisconnected`（`urllib` 与 `requests` 都会遇到）；② HTTP 200 但 JSON 里 `data: null`，akshare 解包时抛 `'NoneType' object is not subscriptable` |
| 是否与标的有关 | **无关**。同一标的连续 4 次重试仍可能全部 `data: null`，换一只立刻成功；反之亦然。纯粹是请求频率函数 |

### 真正的代码缺陷（已修）

1. **`_urllib_get_json` 的 except 类型写错**（`de6b312` / `61976ba` 两轮
   "fine-grained exception handling" 重构误收窄）：
   写成 `except sqlite3.OperationalError` —— HTTP 请求永远不抛 SQLite 异常，
   于是 `URLError` / `RemoteDisconnected` / JSON 解析错误**全部漏网**：
   重试形同虚设、异常直接上抛、失败只剩一条 debug 日志。
   → 已改为捕获 `(urllib.error.URLError, urllib.error.HTTPError, OSError, ValueError)`，
   并在重试耗尽后 `logger.warning`，默认 `retries` 2 → 3。

2. **`fetch_etf_fund_flow` 完全没有重试**：单只失败即 0 行。
   34 只逐只采集（失败率 p）时，连续 5 只全失败的概率在 p=0.3 时约 0.24%，
   在 p=0.4 时约 1%——看起来不高，但**每天跑一次，一个月就会撞上**，
   表现为「ETF资金流: 连续5只失败，跳过剩余29只」→ 整天 ETF 资金流落 0 行
   （2026-09-14 生产日志正是如此）。
   → 已加 3 次重试 + 指数退避（0.8s / 1.6s），并把三类抖动都纳入可重试：
   网络异常、`empty response`、`data: null` 解包异常（`TypeError/KeyError/ValueError/IndexError`）。

### ⚠️ 度量陷阱：不要用「刚压测完的成功率」评估这个修复
本轮排查期间连续高频打 push2his，IP 被阻尼后同一批标的成功率会从 6/10 掉到 3/8。
**这不是修复变坏了**，是自伤。要看真实效果，请对比修复前后**各一个正常交易日**
15:30 定时任务的日志：
- 修前：`ETF资金流: 连续5只失败，跳过剩余N只` + `资金流采集完成: ... ETF0条`
- 修后：期望看到 `ETF资金流(push2his): N 条 (M/34 只)` 且 M 显著大于 0

### 仍未解决（接受）
push2his 的阻尼无法从客户端消除。现有的兜底链保持不变且必须保留：
`逐只 push2his` → `fetch_etf_fund_flow_batch`（datacenter-web 端点，单次请求，
**不受 push2his 阻尼影响**）→ `backfill_etf_fund_flow_from_kline`（K 线估算）。
即「逐只拿历史、批量保当日、K线补空缺」三层，任何一层都不要删。

---

## 问题十一：`portfolio_snapshots` 场外标的存在**非观测的复制行**（fill-forward 陈旧行）

> 记录时间 2026-09-16 · 发现人 risk-manager（复现「相关矩阵跨期收益污染」通报时查出）
> **状态：仅记录，未修。** 本条只登记缺陷本体、全历史范围、连带限制与判别标准；
> `src/analysis/portfolio_risk.py` 的相关性守卫由**另一条线在改**，本文档**不动任何代码**。
> 证据获取：对 `data/database/portfolio.db` 以 `file:...?mode=ro` 只读连接 + 直连东方财富
> `api.fund.eastmoney.com/f10/lsjz`（需 `Referer` 头）取官方单位净值。

### 一、缺陷本体：`2026-06-15 ~ 06-29` 是复制行，且它装的是 `06-12` 的数据

**举证 1 —— 10 只场外标的在金区间内 `current_price` 连续 10 行完全相同**
（且 `quantity` / `market_value` 三项同步不变，故不是「真没涨」）：

| 代码 | 恒定价 | 段内行数 | 段起止 |
|---|---|---|---|
| `519770` | 5.8653 | 10 | 2026-06-15 ~ 06-29 |
| `001437` | 12.2686 | 10 | 同上 |
| `166301` | 16.7360 | 10 | 同上 |
| `001407` | 6.5750 | 10 | 同上 |
| `001323` | 7.5726 | 10 | 同上 |
| `001765` | 2.2150 | 10 | 同上 |
| `002152` | 6.1630 | 10 | 同上 |
| `007994` | 2.6086 | 10 | 同上 |
| `008269` | 1.7130 | 10 | 同上 |
| `100032` | 0.9780 | 10 | 同上 |

（13 只场外里，`001194` 最长段仅 2 行、`027293` 无重复段、`880013` 属设计见问题六。）

**举证 2 —— 该恒定价逐个精确等于官方 `2026-06-12` 的单位净值（10/10）：**

| 代码 | 库内 06-15 价 | 官方净值 06-15 | **官方净值 06-12** | 两者是否相等 | 相对官方 06-15 的偏离 |
|---|---|---|---|---|---|
| `519770` | 5.8653 | 6.3315 | **5.8653** | ✅ | −7.36% |
| `001437` | 12.2686 | 13.4234 | **12.2686** | ✅ | −8.60% |
| `166301` | 16.7360 | 17.4540 | **16.7360** | ✅ | −4.11% |
| `001407` | 6.5750 | 7.0120 | **6.5750** | ✅ | −6.23% |
| `001323` | 7.5726 | 7.9293 | **7.5726** | ✅ | −4.50% |
| `001765` | 2.2150 | 2.3330 | **2.2150** | ✅ | −5.06% |
| `002152` | 6.1630 | 6.4480 | **6.1630** | ✅ | −4.42% |
| `007994` | 2.6086 | 2.6935 | **2.6086** | ✅ | −3.15% |
| `008269` | 1.7130 | 1.7190 | **1.7130** | ✅ | −0.35% |
| `100032` | 0.9780 | 0.9700 | **0.9780** | ✅ | +0.82% |

⇒ **行上写 `06-15`，装的是 `06-12` 的数据**，然后一路复制到 `06-29`。
故 `06-15 ~ 06-29` 这 10 行的场外市值被系统性**低估 −0.35% ~ −8.60%**（中位约 −4.5%），
`portfolio_summary.total_value` 在该区间同步偏低（场外权重约 38.5%）。

**举证 3 —— `06-30` 那行的价逐个精确等于官方 `06-29` 净值，而官方 `06-30` 当日只涨 +3.6%~+5.0%：**

| 代码 | 库内 06-30 价 | 官方 06-29 | 官方 06-30 | 官方当日涨跌 | 快照隐含 |
|---|---|---|---|---|---|
| `519770` | 6.9511 | **6.9511** ✅ | 7.2009 | +3.59% | +18.51% |
| `001437` | 14.4032 | **14.4032** ✅ | 15.0180 | +4.27% | +17.40% |
| `166301` | 19.3660 | **19.3660** ✅ | 20.1100 | +3.84% | +15.71% |
| `001407` | 7.5770 | **7.5770** ✅ | 7.9070 | +4.36% | +15.24% |
| `001323` | 8.2787 | **8.2787** ✅ | 8.6900 | +4.97% | +9.32% |

> **结论：`06-30` 的 +15.7% ~ +18.5% 是「陈旧价 → 真值」的水平修正，不是收益。**
> 同一行 `quantity` 也在变（`519770` 9592.07 → 11786.06，+22.9%），进一步说明它不是单日行情。
> **该行不构成「`06-30` 当日」的有效观测点。**

**这个结论对消费方最要紧的一点：那根跳变的「基期」不是 `06-15`，而是 `06-12`。**
把举证 2 与举证 3 接起来看：`06-30` 行是**官方 `06-29`** 的净值，它的前一行（`06-29` 行）装的是
**官方 `06-12`** 的净值 ⇒

> **被贴上「1 个交易日」标签的那根 +18.51%，实际是 `2026-06-12 → 06-29` 的收益
> （17 自然日 / 11 个交易日）。**

因此任何「相邻行间隔」类守卫必须按**有效基期**（最近一个真值观测）计算 gap，否则
它看到的是 `gap = 1`，会放这条脏观测过去。实测同区间 11 只基金、连续 10 个「交易日」
**价格与份额逐日完全相同**，正是这一段的成因。

**关于序列成因的旁证（发现人 data-engineer）**：`portfolio_snapshots.id` 的插入顺序显示
`07-31` 行（id 37168-37180）**早于** `06-30` 行（id 37226-37237），也早于 OTC 采集器首批（38008+）。
即 `06-30` 是**后补**的一行，`07-31` 是历史月末导入行 —— 这也解释了为什么 `06-30 → 07-31`
这一段是 31 天，以及为什么 `06-30` 会带上「导入时的最新可得净值」。

### 二、全历史范围：**不是孤例，但场外侧仅此一次**（run-length 扫描）

对 `portfolio_snapshots` 全部 36 只标的、全历史扫描「连续相同 `current_price` 的行段」：

| 侧 | 标的数 | 段总数 | 长度分布 | ≥2 | ≥3 | ≥5 | ≥10 |
|---|---|---|---|---|---|---|---|
| **场外** | 13 | 28 | 2 行×17、**10 行×10**、49 行×1 | 28 | 11 | **11** | **11** |
| **ETF（对照）** | 23 | 1074 | 2 行×1025、3 行×44、4 行×5 | 1074 | 49 | **0** | **0** |

**判定：**

- **ETF 侧天然同价的最长段只有 4 行，≥5 行 0 段**（1025 段是「0.001 元最小变动价位 + 低波动」的
  偶然同价，44 段 3 行、5 段 4 行同理）。这给了一个干净的判别阈值。
- **场外侧 ≥3 行的段共 11 段，其中 10 段就是上面同一个事件**（10 只标的在**同一日期区间**同时冻结），
  另一段是 `880013` 的 49 行恒定价 `1.0000`（货币型设计使然，见问题六，**不是缺陷**）。
- ⇒ **`06-15 ~ 06-29` 在场外侧是唯一一次「≥3 行冻结」，但它一次命中 10 只标的**，
  属「**单次事件、系统性影响面**」，而**不是**单标的孤例，也不可当作偶发噪声忽略。

> **精度补充（2026-09-16，按两档判据的行 key `(current_price, round(market_value, 2))` 复算）**：
> 上面这张表用的是**连续相同 `current_price`**，因此 `880013`（恒 `1.0000`）被并进它那条 **49 行**段、
> **看不见它也在这段里被冻结**。换用 `(price, mv 取分)` 这个 key 后，该冻结段的实际覆盖是
> **11 只 × 10 行**（多出 `880013`：它的 `market_value` 在 `06-15~06-29` 同样逐日不变）。
> 即：**「10 只」是「价格」口径下的计数，「11 只」才是「行 key」口径下的计数** ——
> 两个都对，但引用时要带口径。（`880013` 在进入复制行守卫前已被零方差丢弃，见问题六，
> 故两档实际 void 的标的数仍是 **10**。）

**另发现两个「横截面同价簇」（多标的同日同价，与单标的复制行不同类，需分开处置）：**

| 日期区间 | 命中标的 | 特征 |
|---|---|---|
| `2026-06-18 ~ 06-19` | 约 20 只 ETF **同时**出现 2 行同价 | 全市场性 → 大概率是「非交易日 / 全市场未更新」，应按缺失处理而非逐标的清洗 |
| `2026-08-04 ~ 08-07` | `159796` / `159949` / `561910` 各 4 行同价 | 同上，多标的同区间 → 全市场性 |

### 三、⚠️ `2026-08-03` 之后要分「回填段 / 日频写入段」两种写法；**不要做 +1 日对齐**（已实测证伪）

`06-30` 那行恰好等于 `06-29` 的净值，极像「快照行全局滞后一天」。**「全局滞后」已实测证伪：**

> 对 2026-08-03 之后的 7 只场外标的、各 20 个可比日，逐日与官方单位净值比对：
> **`== 净值(D)`：19 天 ｜ `== 净值(D-1)`：1 天 ｜ 两者都不等：0 天。**
>
> **「不要对场外序列做 +1 日对齐」为已实测证伪的结论，请勿重新怀疑并据此去修** ——
> 库里**同时存在两种写法**，全局位移会把**已经正确的回填段**整体推歪。

**2026-09-16 修正：上表那 1 天的性质查清了 —— 它不是「回填段里的异常」，而是换了写入路径的第一个样本。**
「`2026-08-03` 之后对齐当日净值」这句话的适用范围要**收窄到回填段**：

| 段 | 日期 | 写法 | 与官方净值的关系 | 实测（只读生产库 + 东方财富 `f10/lsjz`） |
|---|---|---|---|---|
| **回填段** | `2026-08-03 ~ 09-14` | `scripts/fetch_otc_fund_nav.py`，按 `FSRQ` 对齐；每 code 一个 31 行连续 id 块 | **`== 净值(D)`** | `08-03~08-18`：**134/134** 全中（12 只，除占位价 `880013`）；`09-08~09-14`：**56/56** 全中；7 只各 19 个可比日全中。合计 **190 个可比点无一处 `== 净值(D-1)`** |
| **日频写入段** | `2026-09-15` 起 | **单块 12 行连续 id**（`38408 ~ 38419`），与回填块 id **不连续**（间隔 34 ~ 370） | **`== 净值(D-1)`** | 11 只（`027293` 无 `09-15` 行、`880013` 为占位价）**逐个等于官方 `09-14` 净值**，而官方 `09-15` 全部 ≠ `09-14` |

⇒ **不能笼统写「08-03 之后走 `fetch_otc_fund_nav.py`、精确对齐当日净值」** —— 对齐的是**回填段**；
`09-15` 当天的行拿的是**前一交易日**的净值。

**关于 `09-15`：只有 1 天样本，不能定论。** 已确证的事实：

1. `09-15` 是**一次连续 id 写入**（`38408~38419`，12 行）——与回填的「每 code 一个 31 行块」特征不同；
2. **首日就出现「拿前一交易日净值盖章当日」**，且 **11/11 只同时如此**，不像随机漏取；
3. 旁证（**仅为机制假设，非已证实的结构性滞后**）：`execution_logs` 里日频任务 `portfolio_daily_analysis`
   **每交易日在 `15:30` 启动**（`09-15` `15:30:06`、`09-14` `15:30:15`、`09-11` `15:30:37` …），
   而场外基金当日净值通常当晚才公布 —— 若写入就发生在这一刻，则「能取到的最新净值 = 前一日」是必然。

**待下一交易日验证**（判据）：看库内 `09-16` 行的 `current_price` 等于官方 `09-15` 净值
（⇒ 结构性滞后成立）还是官方 `09-16` 净值（⇒ `09-15` 为单日异常）。
**验证之前：不要据此改写入逻辑，也不要对序列做 +1 日对齐。**
另注：`09-15` 的陈旧价行落在 `2026-08-03` **之后** ⇒ 原文「异常**只限** `08-03` 之前」已不成立
（相关性侧由问题十一 §六 的两档判据兜住，但那不是根治）。

### 四、对 C'（区间摊销口径）的连带限制

C' 的前提是「**断层两端快照都是有效观测**」。把 C' 的 12 段断层摊平明细逐段对位：

| # | `date`（摊平日） | `ref`（锚定日） | k | `delta` (pp) | 端点落在复制行区间？ |
|---|---|---|---|---|---|
| 1 | 2026-02-28 | 2026-01-31 | 15 | +0.5108 | ok |
| 2 | 2026-03-31 | 2026-02-28 | 22 | −1.5274 | ok |
| 3 | 2026-04-30 | 2026-03-31 | 21 | +1.2741 | ok |
| 4 | 2026-05-31 | 2026-04-30 | 19 | +0.5647 | ok |
| **5** | **2026-06-15** | 2026-05-31 | 11 | **−1.3342** | **★ 污染（`date` 落在区间内）** |
| 6 | 2026-07-31 | 2026-06-30 | 21 | −9.4612 | ok（`ref=06-30` 是**真值**，等于官方 06-29 净值） |
| 7 | 2026-08-07 | 2026-07-31 | 5 | −0.0320 | ok |
| 8 | 2026-08-14 | 2026-08-07 | 5 | +0.0062 | ok |
| 9 | 2026-08-21 | 2026-08-14 | 5 | −0.0693 | ok |
| 10 | 2026-08-28 | 2026-08-21 | 5 | −0.0381 | ok |
| 11 | 2026-09-04 | 2026-08-28 | 4 | −0.1058 | ok |
| 12 | 2026-09-11 | 2026-09-04 | 5 | +0.1351 | ok |

- **被污染：1 / 12 段**（`#5`），其 `delta = −1.3342pp`，占 12 段 `delta` 合计（−10.0771pp）的 **13.2%**。
  该段摊的是「把 `06-12` 的数据贴在 `06-15`」的**错位量**，不是收益。
- 影响最大的 `#6`（`−9.4612pp`，占 93.9%）**不受影响** —— 其 `ref = 06-30` 是真值。

> **明确：这条不影响 A / B / C' 三口径 `sharpe_60` 符号一致为负的结论，
> 但影响 C' 作为最终口径的可用性** —— 采用 C' 前必须先修 `#5` 的锚点。

### 五、波动率污染的量化（`portfolio_risk.py:182` 的 `np.diff` 不看日期间隔）

`_analyze_correlations` 取 `current_price`（`:174`）后直接 `rets = np.diff(values) / values[:-1]`（`:182`），
**不看行间日期间隔** ⇒ 跨期行（月末桩 / 复制行）的 30 日收益被当成 1 日收益：

| 代码 | 现状（全行，含跨期行） | 剔 >10 日间隔 | 仅 08-03 后日频段 | 抬高倍数 |
|---|---|---|---|---|
| `001437` | **111.82%** | 64.59% | 57.05% | 1.96× |
| `166301` | **98.68%** | 51.00% | 39.40% | 2.50× |
| `001407` | **96.95%** | 55.50% | 47.92% | 2.02× |
| `519770` | **86.49%** | 56.09% | 38.95% | 2.22× |
| `001323` | **82.43%** | 44.57% | 44.29% | 1.86× |
| `100032` | 30.87% | 19.97% | 11.55% | 2.67× |
| `588000`（ETF 对照） | 60.12% | 60.12% | 44.88% | 1.00× |
| `510300`（ETF 对照） | 21.23% | 21.23% | 14.49% | 1.00× |

**即场外「全行年化波动」被抬高 1.4 ~ 2.7 倍；ETF 侧对照完全无污染（三列相同）。**

> **本小节已在修**（`general-purpose-1` 正在改同一函数：跨期收益 + 折算尖峰 + 零方差 + 静默缺口守卫）。
> 本文档**只登记现象与数值，不动代码**。

> **另一条同类污染（与本表不同源，勿混）**：上面的倍数来自**跨期/复制行**；
> 另有 **13 处「份额折算只入价、未入份额」的真实价格台阶**（相邻两行，单日最大 `r = 3.5581`）
> 同样会被 `np.diff` 当单日收益 —— 清单、判别器与未闭合项见下面 **问题十二**。
> **两者不要混记**：本表列出的污染标的是**场外 6 只 + 2 只 ETF 对照**，
> 而问题十二的 13 例是 **7 只 ETF + 6 只场外**，**与本表列出的标的不重叠** ⇒ 是**两条独立**的污染源，
> 修好本表那条（跨期间隔）**不会**顺带修好折算台阶那条。

### 六、判别标准的形式化写法（供将来写进代码）

```python
# (1) 标的级主判据 —— 「复制行段」
#     条件：连续同价 且 quantity/market_value 同步不变 且 段长 >= 5
def is_replica_run(run) -> bool:
    return (run.price_const and run.qty_const and run.mv_const
            and run.length >= 5)
#     阈值 5 的依据：ETF 侧（23 只、3477+ 行）天然同价最长仅 4 行、>=5 行 0 段；
#     场外缺陷段为 10 行 —— 阈值 5 可把「真没涨」与「复制行」完全分离。

# (2) 横截面判据 —— 「全市场未观测日」
#     条件：某交易日 >=50% 的标的与上一交易日价格完全相同 => 该日按缺失处理
#     （用于 2026-06-18~06-19、2026-08-04~08-07 这类多标的同价簇，勿逐标的清洗）

# (3) 白名单豁免
#     货币型（MONEY_FUND_CODES，如 880013）价格恒 1.0 属设计（见问题六），
#     必须先豁免，否则它的 49 行长段会被误判为复制行。

# (4) ⚠️ 间隔类守卫必须按「有效基期」计算 gap（本条最易漏）
#     维护 last_real_date：遇到「与上一行 (price, quantity) 完全相同」的行时不刷新它，
#     该位置收益记 NaN（复制行内部收益恒为 0，是伪造点）；
#     对每个产出收益的位置，gap = 该行 date − last_real_date。
#     —— 因为复制行把基期「顶」到了 06-12，而表面 gap 只有 1 天：
#        按表面 gap 算 => 06-30 那条是 gap=1，被放行；
#        按有效基期算 => 06-30 的 gap = 18 自然日（06-12→06-30）⇒ 被跨期闸门拦下。
#     注意：不要另立「零收益即丢弃」规则 —— 会误杀真实的平价日。
```

> **⚠️ 本段（判据 (4)）的 `18` 与「基期 06-12」经实测更正 —— 上面的代码块保留原文，但不要当读数用。**
> **2026-09-16 源码级实测**（复用生产模块自身：`_scan_replica_rows` + `:504-526` 循环 + 真 `is_otc_fund`）：
> `06-30` 的 `gap` **逐 `code` 不同，实测 `{15, 30}`** —— `15`（5 只，基期 `06-15`）/ `30`（5 只，基期 **`05-31`**）。
> - **`18` 不是代码产出**：`_calendar_gap_days('06-12','06-30') = 18` 这个**函数值**存在，
>   但**没有任何 `code` 在 `06-12` 有快照行**（那 5 只的最早行就是 `06-15`）
>   ⇒ `18` 既不是 `last_real_date`、也不是任何实际行日期。
> - **「复制行把基期顶到 `06-12`」这个说法也不准确**：基期是**行日期**，不是**值日期**；
>   `06-15` 那一行装的是 `06-12` 的净值，但它的**行日期是 `06-15`**，而它自身被 `Tier1` 含段首判为非观测
>   ⇒ 基期回退到该 `code` 的上一个真观测行（`05-31`）或窗口首行（`06-15`）。
> - **方向结论不变**：`15 / 30 / 18` **都 `> 12`**，所以「被跨期闸门拦下」成立。
>   变的只是**引用时必须写明的那个数**。
>
> 详细分解与两张表见本文 §三 之后的「月末批」小节。

配套断言（若将来加校验）：任一标的在**任意 60 交易日窗口内**出现 `>= 5` 行的复制行段
⇒ 该标的在窗口内的收益序列**整体标记不可信**，并从相关性 / 风险聚合中剔除或降级展示。

#### 实现状态（2026-09-16 更新）

> **判据的决策留痕与各提交哈希台账另见 `docs/handover/10_correlation_guard_decision_log.md`**
> （含 `07fa3e3` → `9de2800`（B2）→ `05c7288`（两级落地）→ `6434f05` 的时间线与 blob 哈希）。
> 两份文件**口径必须一致**，冲突时以决策留痕文件为准。

> **上述判别标准 (1)(2) 的去向（2026-09-16 二次更正）**：它们当天的确被判为「不落地」，
> 中间落过一版 `B2`，**最终又回到「(1) 段长 + (2) 横截面广度」的组合** ——
> 即下面 `Tier1` / `Tier2`。所以 **(1)/(2) 不是被否决的设计，而是最终实现**（差异只在判等的 key
> 与阈值依据，见下）。
> 本节**保留原判据原文与 `B2` 时期的注记**，因为那是当天的推理记录；**不要因为它们与现状不同就删掉**。

**最终落地的是「两档」实现**（`COPY_RUN_MIN_LEN = 5` / `COPY_BREADTH_MIN_N = 3` /
`横截面占比阈值 = 50%（硬编码 c*2<n）），行 key 统一取 `(current_price, round(market_value, 2))`（`_replica_key`）：

> **Tier1 · 段长（数据驱动，不加 `is_otc_fund` gate）**：某标的自身序列里 key 相同的连续行构成一段，
> 段长 `L >= COPY_RUN_MIN_LEN(5)` ⇒ 该段**全部 L 行（含段首）**都不是观测。
> 不加 gate 的理由：该判据对 ETF **零影响**（`>=5` 的段在 ETF 历史上不存在）。
>
> **Tier2 · 横截面广度（仅场外篮子内，用 `config.settings.is_otc_fund`）**：对日期 `D`，
> `n` = 「在 `D` 有行、且自己上一行也存在」的可比较场外标的数，
> `c` = 其中 key 与自己上一行相同的只数；若 `n >= COPY_BREADTH_MIN_N(3)` 且
> `c / n >= 50%（硬编码 c*2<n）` ⇒ 这 `c` 只在 `D` 的行都不是观测。
>
> 两档命中者一律**判为非观测、基期回退到最近真值观测**；`Tier1` 优先级更高（保留其层级标注）。

**两档各自补的洞**（实测）：

- **`Tier2` 的主理由 —— 抓「只有 2 行的全市场事件」**：`2026-09-14 → 09-15`，场外与各自上一行同 key 的
  是 **12 只**（`c/n = 12/12 = 100%`），**段长性质完全抓不到它**（每只都只有 2 行，永远够不到 `L >= 5`）。
  ⇒ 这是 `Tier2` **不可替代**的理由：**长段能用段长抓，短段只能靠横截面广度。**
  （注意「分母 12 / 命中 12 / 实际 void **11**」三者的差异：分母与命中来自**第一层预扫描**（含 `880013`），
  void 登记在**第二层**且 `880013` 已被零方差短路 —— 见下「实现顺序」。）
- `Tier1` 抓**长复制段的段首** —— 段首装的同样是陈旧值（`06-15` 那行装的是官方 `06-12` 净值），
  而「相邻两行同价」会漏掉段首。

**两档之前的 `B2` 是什么**：中途一版（`9de2800`），只按「`(price, market_value 取到分)` 与上一行相同」
逐行判定、并对整体判据加 `is_otc_fund` 门控（等于放弃 ETF 侧、且放弃段长与广度两条结构性证据）。
**它已被其后提交取代，取代它的直接理由见下面「改判据的直接理由」—— 那 6 条真实平价观测会被它一并 void。**

**附：旧稿的一处归因错误（2026-09-16 更正，与判据选择无关）**：旧稿写「`100032` 三条…（疑为真实平价日）」，
其依据 `+0.82%` 是**比错日期**的产物 —— 该段恒定价 `0.9780` 与其余 9 只一样，
**精确等于官方 `2026-06-12` 单位净值**（10/10 逐位相同，含 `100032` 的 `0.9780`）；
旧稿拿它去比官方 `06-15`（`0.9700`）才得出「方向不一致」，而**该段基期是 `06-12`**，
比 `06-15` 没有意义。⇒ `100032` 与其余 9 只**同源**，不存在例外。

**改判据的直接理由：B2 会误 void 真实平价观测 —— 逐条实测 6 条。**
按「相邻两行同价」这一**单名条件**做**全历史扫描**（13 只场外，无窗口限制），共命中 **117 行**：
非观测 **111** 行（`06-17~06-29` 恒定价 **99 行 = 11 标的 × 9 天** + `09-15` 陈旧价行 **12 行**）void 正确；
另 **6 行为真实观测**（被 B2 误 void）：

| 代码 | 日期 | 库内 `current_price` | 官方净值(D) | 官方净值(D−1) |
|---|---|---|---|---|
| `100032` | `2026-08-05` | `0.9560` | `0.9560` | `0.9560` |
| `100032` | `2026-08-21` | `0.9760` | `0.9760` | `0.9760` |
| `100032` | `2026-09-01` | `0.9950` | `0.9950` | `0.9950` |
| `001194` | `2026-09-09` | `6.1260` | `6.1260` | `6.1260` |
| `001407` | `2026-09-09` | `5.9250` | `5.9250` | `5.9250` |
| `002152` | `2026-09-09` | `6.0370` | `6.0370` | `6.0370` |

（`111` 行中含 `880013` 占位价 **10 行** —— `06-17~06-29` 9 行 + `09-15` 1 行，属问题六。
**该顺序已确认：`880013` 不计入 void**，见下「实现顺序」。）

**可离线复现的判别量：横截面广度 `c/n`**（发现人 data-engineer，建议长期复用 ——
「某日场外篮子内，与各自上一行 key 相同的只数 `c` / 可比只数 `n`」）。
它在三类样本上的取值分得非常干净，**不需要联网核官方净值**：

| 日期 | `c/n` | 占比 | 性质 |
|---|---|---|---|
| `2026-06-17 ~ 06-29` 各日 | `11/11` | **1.00** | 全篮子陈旧写入 ⇒ 该 void（已由 `Tier1` 判定，`Tier2` 不重复登记） |
| `2026-09-15` | `12/12` | **1.00** | 同上（本例由 `Tier2` 判定） |
| `2026-09-09`（3 只） | `3/12` | 0.25 | **少数** ⇒ 真实平价日，**不该 void** |
| `2026-08-05` / `08-21` / `09-01` | `1/12`、`1/13`、`1/12` | **0.08** | **单只** ⇒ 同上 |

⇒ 它**替代不了官方净值**，但**比「段长」更接近成因证据**（陈旧写入总是**全篮子同时发生**），
且可离线复现 —— 这也正是横截面同值占比阈值 `0.5` 这个阈值有分辨力的原因。

**全量普查（2026-09-16 实测，独立复算）：`c/n` 是双峰的，`50%` 那条线一天都没承重。**
把**全部可比日**（**48 天**，范围 `2026-02-28 ~ 2026-09-15`）逐日打出 `n / c / ratio`：

| 分组 | 天数 | `ratio` 取值 |
|---|---|---|
| 触发 VOID | **10 天**（`06-17~06-29` 九天 + `09-15`） | **全部恰为 `1.00`**（全篮同值） |
| **不触发**、但 `c > 0`（有横截面证据） | **4 天** | `0.0833`（`08-05` `1/12`）、`0.0769`（`08-21` `1/13`）、`0.0833`（`09-01` `1/12`）、**`0.25`（`09-09` `3/12`，为最大非 VOID 占比）** |
| 不触发、且 `c == 0`（**完全无横截面证据**） | **34 天** | `0.00` |

⇒ 观测到的「**非 VOID 上界**」= **`0.25`**、「**VOID 下界**」= **`1.00`**；
**阈值在 `(0.25, 1.00]` 区间内取任何值，这 48 天的裁决完全相同。**
⇒ **判据在观测样本上等价于「全篮同值 vs 非全篮同值」的二值判断** ——
**不存在 `30%` / `45%` 这类中间态被裁决的案例**（实测一个都没有）。

> **⚠️ 这条必须写进文档，否则读者会误以为中间态被处理过。**
> 推论两条：
> 1. **横截面同值占比阈值 `0.5` 是「非承重」的（原 `COPY_BREADTH_MIN_RATIO` 常量已由 #87 裁定 B 删除），不只是「未被消费」** ——
>    代码里判定走硬编码 `c*2 < n`（原常量只进日志，#87 裁定 B 已删此常量），
>    但**即使真的消费它，观测样本上的行为也完全一样**。收口时的风险说明应按「非承重」措辞，不按「无影响」。
> 2. 保留 `0.5` 的价值**不在历史分辨力，而在对未来中间态的防御** ——
>    现有 48 天里中间区间是空的，但它**空得没有理由保证将来还空**。
>
> **口径（必带）**：窗口 = 每 `code` `get_price_history(code, 60)`（**`LIMIT 60` 行，不是 60 日历天**）；
> 场外篮子 = `is_otc_fund` 为真的 13 只；`key = (current_price, round(market_value, 2))`。
> 我按模块语义**自写脚本独立复算**（不复用对方脚本），**48 天逐日与 `fix-copy-two-tier` 的实测逐项一致**
> （含那 4 天非零占比与 34 天 `c = 0`）。

**⚠️ 别把「月末批伪跳变」和上面这两条判据的时间轴混在一起。**

月末批 `2026-06-30` 与 `2026-07-31` 合计 **`10 + 10 = 20` 只**（**我的口径**：进了扫描、且在窗口内有 `gap` 位置的只数；
若按「当日有行的场外只数 − `880013`」口径则是 `11 + 11 = 22`，见下方只数分解表），
**从来没进过相关矩阵** —— 它们在**产出收益之前**就被跨期闸门接走了，**不是"进了矩阵后被某级判据拦下"**。
下面是**源码级实测定稿**（不是转述、不是估算）：

> **✅ 已定死（2026-09-16，源码级实测）**：直接 `import src.analysis.portfolio_risk`，
> 复用它的 `_scan_replica_rows` 取 `void_map`、走它 `:504-526` 的循环、用真的 `config.settings.is_otc_fund` 取篮子。
> 结论是 **`06-30` 的 `gap` 不是一个数，而是逐 `code` 的 `_calendar_gap_days(last_real_date, '06-30')`，实测 `{15, 30}`**：
>
> | 目标日 | `gap` 取值 | 只数 | `code` | 基期 | 去向 |
> |---|---|---|---|---|---|
> | `06-30` | **`15`** | `5` | `001323`/`001407`/`001437`/`002152`/`519770` | `06-15`（**窗口首行**，其自身是段首、被判 void） | 跨期 void |
> | `06-30` | **`30`** | `5` | `001765`/`007994`/`008269`/`100032`/`166301` | **`05-31`**（月度真观测行，`last_real_date` 在此前移） | 跨期 void |
> | `07-31` | **`31`** | `10` | 上面 10 只同名单 | `06-30`（该行 `c=0`、非 void ⇒ 真观测） | 跨期 void |
>
> ⇒ **`06-30` 的两种 `gap` 来自两种不同的基期来源，都是代码产出**：
> `15` 组是「窗口首行即基期、且首行自身是 `Tier1` 段首」；`30` 组是「月度真观测把基期推到 `05-31`」。
> `06-15` 两组都**不是**真观测（`Tier1` 含段首），所以它**不能**当基期 —— 这正是 `07` 同一条判据 b) 的落地效果。
>
> **⚠️ 两处必须更正（都被实测否掉）：**
> 1. **判据 (4) 那段写的 `18`（`06-12` → `06-30`）不是代码产出。**
>    `_calendar_gap_days('2026-06-12','2026-06-30') = 18` 这个**函数值**存在，
>    但**没有任何 `code` 在 `06-12` 有快照行**（`001323`/`001407` 等的最早行就是 `06-15`）
>    ⇒ `18` 既不是 `last_real_date`、也不是任何实际行日期，**只能来自"按值日期推导"的叙述**。
>    ⇒ 判据 (4) 那半句属**设计意图描述**，**不要当读数引用**。我已把它在原文处标注。
> 2. **`150` 也不是代码产出** —— 它是「拿**窗口首行**直接算到 `06-30`」的伪值
>    （`_calendar_gap_days('2026-01-31','2026-06-30') = 150`），**忽略了 `last_real_date` 会在月度真观测行前移**。
>    真实基期是 `05-31`，`gap = 30`。**引用 `150` 会得出"基期是 01-31"这种与代码相反的结论。**
>
> **口径（必带）**：上表 `只数` 是「**进了扫描、且在窗口内有 `gap` 位置的只数**」，两个日期**都等于 `10`**，
> 但 `10` 的**来源不同**（都从 `13` 只 `is_otc_fund` 起算）：
> - `06-30` 的 `10` = `13` − `027293`（`09-15` 无行 ⇒ 不进本轮扫描篮子）− `880013`（`zero_variance` 短路）
>   − `001194`（**它的窗口是 `07-31..09-15`，根本不含 `06-30`**）。
> - `07-31` 的 `10` = `13` − `027293` − `880013` − `001194`（**它的窗口首行就是 `07-31` 本身 ⇒ 那一行是基期、没有 `gap` 位置**）。
> ⇒ 两个日期的 `001194` 都落榜，但**原因不同**（一个在窗外、一个在窗口首行当基期）。
> ⇒ `fix-copy-two-tier` 报的 `11` 是「**当日有行且没被短路的只数**」（把 `001194` 算进去了）。
> **`10` 与 `11` 都对，差在 `001194` 算不算"批内"**（它有行、但那一行是基期、不产出收益）—— 引用时必须说明。

> **唯一残余**：`07-31` 那一行会作为 `08-03` 收益的**基期** ⇒ 该位置跨 **2 个交易日**（`3` 个自然日）。
> ⇒ 说「月末批伪收益」时**不要**写成"靠两级判据拦下" —— 两级判据拦的是**别的日期**：
> **`Tier1` 拦 `06-17~06-29` 的照抄段，`Tier2` 拦 `09-15` 的全篮陈旧。三条时间轴互不重叠。**
>
> **⚠️ `gap` 必须按「有效基期」算，不能按表面相邻行**（这正是本文 §六 判据 (4) 那条坑的活样本）：
> 若按**表面**取该标的在 `06-30` 之前的最近一行，`06-17~06-29` 是照抄行、会被当成真观测
> ⇒ `gap` 只剩 **`1` 天**、跨期闸门**放行**（我第一版复算就是这么算的，得到"`11` 只 `gap=1`"的错结论）。
> 按**有效基期**（`last_real_date` 不刷新）才算得出 `15 / 30` 天。
> **同一个日期、两个口径，差 `15` 倍** —— 这正是本节反复强调「凡数字必带口径」的原因。
>
> **口径对齐说明（只数分解，实测）**：`13 / 12 / 11 / 10` 是四个不同的分母，**必须分开说**：
>
> | 日期 | `is_otc_fund` 篮子 | 减 `027293`（`09-15` 无行 ⇒ 不进扫描篮子） | 减 `880013`（`zero_variance` 短路） | 减 `001194`（窗口内无 `gap` 位置） | 扫描中产出 `gap` 的只数 |
> |---|---|---|---|---|---|
> | `06-30` | `13` | `-1` → `12` | `-1` → `11` | `-1` → `10`（它的窗口 `07-31..09-15` **不含 `06-30`**） | **`10`** |
> | `07-31` | `13` | `-1` → `12` | `-1` → `11` | `-1` → `10`（**窗口首行即 `07-31`，该行是基期**） | **`10`** |
>
> ⇒ 若把分母写成「**当日有行的场外只数 − `880013`**」会得到 `11`（两个日期都是 `11`），
> 与「扫描中产出 `gap` 的只数」`10` 差 `1` —— **差的永远是 `001194`**。
> **报数时必须写清是 `11` 还是 `10`，以及 `001194` 算不算"批内"。**

> **⚠️ 分母 `n` 会变，不是笔误**（上表里 `1/12` 与 `1/13` 并存的原因）：
> `n` = 当日「**有行、且自己上一行也在窗口内**」的场外只数，而窗口是 `days = 60` **行**（不是日历天），
> 所以**各标的回溯到的起点不同**（那段代码里最长的可达 49 行、回溯到 `01-31`；`001194` 只有 33 行、从 `07-31` 起）。
> 实测分母只有 `6 / 11 / 12 / 13` **四个取值**（下表四段中，后两段同属一个日期段、只是周五多一只）：
>
> | 日期段 | `n` | 为什么是这个值 |
> |---|---|---|
> | `2026-06-15` | **6** | 只有回溯到 `01-31` 的 6 只，其「上一行」才也在窗口内 |
> | `2026-06-17 ~ 06-29` | **11** | `001194`（窗口从 `07-31` 起）与 `027293`（从 `06-30` 起）都不在 |
> | `2026-08-03 ~ 09-15` 的非周五 | **12** | 上面 12 只可比（含 `880013`） |
> | `2026-08-03 ~ 09-15` 的**周五** | **13** | 多出 `027293` —— 它**只按周落行**，只有恰好跨一周时它才有「上一行」可比 |
>
> 所以那 6 条真实平价观测的占比分别是：`08-05` = `1/12 = 8.3%`、**`08-21` = `1/13 = 7.7%`**、
> `09-01` = `1/12 = 8.3%`、`09-09` = `3/12 = 25.0%`（`09-09` 不是周五）。
> **四者都远低于 `50%` ⇒ 结论不受分母影响**；但**引用单个 `c/n` 时必须连日期一起引**，否则会被当成算错。

> **可复用的验收不变量：`overlap_days` 场外合计**（发现人 data-engineer）。
> 它直接读数「放生了多少真实平价日」，比数 flag 条数更直观：
>
> | 判据 | `overlap_days` 场外合计 | 含义 |
> |---|---|---|
> | `base`（无复制行判据） | `452` | 含全部伪零收益 ⇒ **漏判** |
> | `B2` | `335` | 把 6 条真实平价日也 void ⇒ **误伤** |
> | **两档（现行）** | **`341`** | 6 条保留 ⇒ **正确值** |
>
> 三个数**互相自洽、可当场验算**：`452 − 111(= 两档 void) = 341`、`341 − 6(= 真实平价日) = 335`、
> `341 = 11 × 31`（11 只在场外篮子内、每只 31 个有效收益日；`880013` 整只被 `zero_variance` 丢弃记 `0`、
> `027293` 周频不计入）。
> ⇒ **以后验证「有没有误 void 真实观测」，看这个合计是否 = `341` 即可**（`335` 即误伤、`452` 即漏判）。
> 口径：`_analyze_correlations` 返回值里的 `overlap_days` 字典、只对 `is_otc_fund` 为真的标的求和；
> 副本 `portfolio.db.bak_verify_corr_20260916_095652.db`、快照日 `2026-09-15`、`days = 60`（行数）。
> （`341` 本身为 data-engineer 实跑值，我**未重跑相关管线**独立复核，只做了上面的算术自洽性核对。）

> ⚠️ **上句引用的副本已归档（注于 2026-09-17；不改写上述结论，结论仍按原文成立）**：
> `data/backups/portfolio.db.bak_verify_corr_20260916_095652.db` 已按备份保留策略 **gzip 归档**，
> 现路径 **`data/backups/archive/portfolio.db.bak_verify_corr_20260916_095652.db.gz`**
> （141,017,088 B → 46,520,636 B；解压后 sha256 与原件逐字节一致，`261a4a6ed03396cd…`）。
> 需重跑该副本时请先解压到 `data/backups/` 再按原命令执行。

**聚合影响〔按 `B2` 口径；两档下这 6 条**不**被 void，故下列影响实际不发生，仅作 B2 代价的记录〕**：这 6 条的当日收益本就为 `0`（官方净值与前一日**逐位相同**），void 后该日记 `NaN`，
而 `cur == prev` ⇒ **后续行收益逐位不变** ⇒ 对**累计收益与风险三列的影响为 `0`**；
相关性侧只是这 6 个 `(标的, 日期)` 的重叠日各少 1 天（当日权重实测 `0.26% ~ 5.06%`）。

> **两档实现已消除这 6 条**（`9de2800` 的 B2 已被其后提交取代）：
> `Tier1`（段长 `>= 5`，**含段首**）与 `Tier2`（横截面占比 `>= 50%`）都不命中这 6 行
> （段长 `<= 2`；横截面占比 `08-05` = `1/12 = 8.3%`、`08-21` = `1/13 = 7.7%`、`09-01` = `1/12 = 8.3%`、
> `09-09` 三条 = `3/12 = 25.0%`）；
> `portfolio_risk.py` 的注释亦把它们列为「相邻两行同价」判据的误伤样本。
> ⇒ 按两档实现，**「真实观测被误 void」这项代价为 0**。
> `Tier1` 含段首会**额外** void `06-15` 的 **10 行**段首（10 只，其价 == 官方 `06-12` 净值，**同属非观测**，
> 实测 `001323` 库内 `7.5726` vs 官方 `06-15` `7.9293`），**不构成代价**。

**两档下的 void 行数（权威值 = 生产副本实跑；我已独立复现，两条路逐项一致）**：

| 口径 | 两档 void 合计 | `Tier1`（段长） | `Tier2`（横截面） |
|---|---|---|---|
| `snapshot_date = 2026-09-15`、34 只持仓、入矩阵 33 只、每标的窗口 = `get_price_history(code, 60)` | **111 行** | **100 行**（**10 标的 × 10 日期**：`06-15`、`06-17~06-29`） | **11 行**（11 标的，全部落在 `09-15`） |

同一次运行的旁证：`matrix_dim = 33`、`copy_flag_total = 111`、`affected_labels = 11`、
`skipped_codes = [880013 天添利 / 零方差 / 49 行]`、`unreliable_codes = []`
（折算点都在扫描窗外 ⇒ **空是正确结果，不是守卫失效**）。

> **按修订对照（纪律：引用任何计数都必须钉住被测修订）** ——
>
> | rev | copy flag 场外 | `overlap_days` 场外 | 场外零收益 | `average_correlation` | `high_correlation_pairs` |
> |---|---|---|---|---|---|
> | `9de2800`（B2） | `107` 条 / 11 只 | `335` | `0` | `0.4113` | `102` |
> | `05c7288`（两级首版） | **`106`** 条 / 11 只 | `341` | `6` | `0.4111` | `101` |
> | **`6434f05`（= 现 HEAD）** | **`111`** 条 / 11 只 | `341` | `6` | `0.4111` | `101` |
>
> **`106 → 111` 的 `+5` 是什么**：只差 5 只各 `+1` ——
> `001323`/`001407`/`001437`/`002152`/`519770` 的 **`06-15` 窗口首行段首留痕**
> （`2026-06-15 Tier1 段长=10（窗口首行=基期，无收益位置）`），其余 6 只不动。
> 而 `overlap_days` / 零收益 / `average_correlation` / `high_correlation_pairs`
> **在 `05c7288` 与 `6434f05` 上完全相同** ⇒ **段首留痕只增留痕条数、不改收益序列**
> （这是 **rev 级证据，不是推测**）。
> ⇒ **`106` 可以引用，但必须写成「rev `05c7288` 的值」并注明已被 `6434f05` 取代为 `111`**；
> 孤立地写 `106` 会与 `111` 看起来冲突。rev 级 blob 哈希台账见 `10_correlation_guard_decision_log.md`。

> ⚠️ **口径要点（易错）**：窗口参数 `days = 60` 是**行数**，不是日历天数 ——
> `get_price_history` 的实现是 `ORDER BY date DESC LIMIT days`。
> 所以**每个标的的扫描日期跨度不同**：只有月末行的 6 只场外回溯到 `01-31`（49 行），
> `001323`/`001407`/`001437`/`002152`/`519770` 回溯到 `06-15`（44 行），
> `001194` 只有 `07-31` 起的 33 行（**所以它没有 6 月块**），`027293` 8 行。
> 这直接决定 `Tier1` 的段长与 `Tier2` 的分母 `n`。

> ⚠️ **两个 `111` 不是同一件事**：B2 口径的 `111` = 单名条件命中 `117 − 6 条真值`；
> 两档口径的 `111` = `Tier1 100 + Tier2 11`。**同值纯属巧合，构成完全不同** ⇒ 引用必须带口径。
> ⚠️ **计数的完整闭环（含段首）**：上面的 `117` 是「相邻两行同价」单名条件的命中数，
> 它**扫不到 `06-15` 段首**（段首没有可比的上一行）。补上段首才是全部同位行：
>
> | 口径 | 分解 | 合计 |
> |---|---|---|
> | 单名条件全历史扫描 | 非观测 `111`（`06-17~06-29` 恒定价 `99` + `09-15` 陈旧价 `12`）+ 真实观测 `6` | **`117`** |
> | 同上 **+ `06-15` 段首** | `117` + `11` 行段首（段首归**非观测**侧） | **`128`** |
> | 两档实现（生产副本实跑） | `Tier1 100`（10 标的 × 10 日期，**已含 `06-15` 段首 10 行**）+ `Tier2 11` | **`111`** |
>
> **段首归「非观测」侧**：`06-15` 那 11 行的 `current_price` 逐个精确等于官方 `2026-06-12` 单位净值
> （实测 `001323` 库内 `7.5726` vs 官方 `06-15` `7.9293`），与它后面 10 行装的陈旧值**同源**。
> ⇒ `117 + 11 = 128` 与上表口径 B 的 `111` **不矛盾**：前者是「单名条件扫描 + 段首」，
> 后者是「两档实跑」——两档实跑的 `100` 里已经把段首算进去了，且它不含 `880013`
> （该只被更早触发的 `zero_variance` 短路，故不产生任何 void 留痕）。

**`122` 与 `111` 是「两层」，不是一个数 —— 此前我把 `122` 当「作废的数」删掉，是错的（2026-09-16 二次更正）**：

| 层 | 范围 | 命中 | 是否登记为 void |
|---|---|---|---|
| **第一层** `_scan_replica_rows` 预扫描（**全标的，含 `880013`**） | `Tier1` 11 只 × 10 天 = `110`；`Tier2` 12 只 × 1 天 = `12` | **`122` 行 / 12 只** | 否（只是**扫描结果**） |
| **第二层** 逐标的循环登记（`880013` 先被 `zero_variance` 短路） | `100 + 11` | **`111` 行 / 11 只** | 是（`copied_rows_voided`） |
| **差额** | — | `11` 行 = `880013` 的（`Tier1 10 + Tier2 1`） | — |

⇒ **两个数都对，只是层不同**；错的只是我一开始把**预扫描层的数标成了 void 数**、随后又**把数本身当错的删掉**。
现已按两层写回 —— 这比删掉更有价值，因为它把两层结构留在了文档里（gp-1 与我的脚本各自独立复现到同一组值）。

> 📌 **`docs/handover/10_correlation_guard_decision_log.md` 里那个 `122` 与本节的 `122` 是同一批行**，只是两种说法：
> 口径 A 说「单名条件命中 `117 − 6 真实 = 111`，再加 `06-15` 段首 `11` ⇒ `122` 条非观测」；
> 预扫描层说「`Tier1 110 + Tier2 12`」—— **行集合相同**（「11 只 × `06-15`/`06-17~06-29` 共 10 个日期」∪「12 只 × `09-15`」）。
> ⇒ 引用 `122` 时说明**是哪一层/哪种口径**即可，两处不会冲突。
> （同理，`111` 也有两个口径：B2 的 `117 − 6` 与两档登记的 `100 + 11` —— **同值不同构成**，引用同样要带口径。）
>
> 另一条独立教训：**复现别人的口径时，先确认参数语义（这里的 `days` 是「行数」），再确认执行顺序** ——
> 否则差值会伪装成「数据差异」。我第一版复现就是在这两点上同时出错（按日期切窗口 + 没模拟第二层短路），
> 才把预扫描的 `122` 误当成 void 数。

**实现顺序（已查代码确认）**：

```
_scan_replica_rows(所有 prepared 标的)   ← 第一层：全标的预扫描（含 880013），
                                            算出 Tier1/Tier2 命中 + Tier2 的横截面分母
for p in prepared:                       ← 第二层：逐标的循环
    if n_raw < 2 → skip
    ... zero_variance 命中 → 直接 continue（不再走到 void 登记）
```

⇒ **扫描在前、void 登记在后；`zero_variance` 在第二轮短路。** 三处后果逐一落定：

1. **void 数不含 `880013`**：`111 = Tier1 100 + Tier2 11`（它的 11 行被短路掉）；
2. **`Tier2` 的分母 `n` = `12/12 = 100%`**（**不是** `11/12`）—— 分母来自**第一层**，
   而第一层覆盖全部标的（含 `880013`）；`880013` 只在第二层被短路，**不影响分母**。
   ⇒ 「**分母含 `880013`、void 不含 `880013`**」这个不对称是设计使然，不是 bug；
3. **无重复登记**：`880013` 不在 `copied_rows_voided` 里（第二轮 `continue` 掉了），
   所以「同一标的被两类规则各记一次」的情形**不成立**。

**与 (1) 的差异〔本小节为 `B2` 时期的判断，已被两档实现取代 —— 保留原文，见其后「结论反转」〕：
不使用 `length >= 5` 段长阈值。** 原因（实测）：

| 代码 | 段前半 `market_value` | 段后半 `market_value` | `quantity` |
|---|---|---|---|
| `001407` | 55498.0（06-15..06-18） | 55497.997（06-19..06-29） | 8440.76 全 10 行逐位相同 |
| `166301` | 81672.52 | 81672.51680000001 |  |
| `001437` | 44034.21 | 44034.213747999995 |  |
| `519770` | 56260.37 | 56260.368171 |  |

`market_value` 存在**亚分位浮点抖动**，故精确 pair 判等会在 `06-19` 处断链、把该行误判为
「真观测」，基期刷新到 `06-19` ⇒ `06-30` 的 gap 只剩 **11 天 ≤ 12** 被放行。
即：**段长阈值这条路线在这 4 只目标标的上恰好失效**（`base` 档对这 4 根一根都没拦下）。
⇒ 改为 `market_value` **取到分**后判等，并在场外侧专用（消除抖动与 ETF 侧自然同价误伤）。

> **结论反转（2026-09-16，两档实现定稿）：段长阈值这条路线回来了，就是 `Tier1`。**
> 上面那个「恰好失效」的**根因是判等的 key 选错了，不是段长阈值本身错**：
> key 从「精确 pair」改为 `(current_price, round(market_value, 2))`（`_replica_key`）后，
> `06-19` 的亚分位抖动不再断链，`06-15~06-29` 是一条完整的 **10 行**段（`L >= 5`）⇒ 整段连带**段首**一起 void。
> 因此两处被同时修掉：① 4 只目标的 4 根假跳变**这次被拦下**；② 不再需要 `is_otc_fund` 去兜「ETF 侧自然同价」——
> 实测 `>=5` 的段在 ETF 历史上**不存在**（见本文问题十一 §二：ETF 侧全历史最长自然同价 4 行、`>=5` 为 0 段），
> 所以 `Tier1` **不需要** gate。**「取到分」这一手保留在 key 里**（抖动之解），
> 而**它不再单独承担判据** ⇒ 不再有「单只真实平价日被误 void」的副作用。

**与 (2) 的差异〔本小节为 `B2` 时期的判断，已被两档实现取代 —— 保留原文，见其后「结论反转」〕：
未实现横截面广度判据。** B2 的结果与另一条线用**完全不同的方法**
（特例签名）独立算出的 `0.4112 / 101` 只差 `1e-4` / 1 个标对 —— 为 `1e-4` 再加一层机制不值当。

> **结论反转（2026-09-16，两档实现定稿）：横截面广度已实现，就是 `Tier2`。**
> 当年「不值当」的推理有个漏洞：**「只差 1 个标对」不能用来判断口径是否等价** ——
> `high_correlation_pairs` 是 `|ρ| > 0.8` 附近的**边界整数计数**，实测它对窗口长度与阈值都敏感
> （`days`：`55→102 / 59→101 / 61→104`；阈值：`>0.795→107 / >0.805→99`），
> **`1e-4` 级的均值扰动就足以跨过 1 对标对**。所以 `average_correlation` 是**多条独立实现一致的证据**，
> `101` vs `102` 不是（详见优先级表「十一」那一行的口径注）。
> 真正让 `Tier2` 必须存在的理由是：**`09-14 → 09-15` 只有 2 行**的全市场事件，段长性质抓不到，
> 只能靠横截面广度。**这条洞在 `B2` 里是被 `is_otc_fund` 门控顺带堵住的**，
> 而两档去掉了整体门控 ⇒ 必须把广度判据显式补上。

> **当初差点把 `Tier2` 砍掉 —— 方法论记录（留档以免重犯）**：当时的理由是
> 「`B2` 的结果与另一条线只差 `1e-4` / `1 个标对`，**为 `1e-4` 再加一层机制不值当**」。
> 这一步推理是**错的**：它拿一个**边界敏感的整数**（`high_correlation_pairs`，实测值在 `|ρ| > 0.8`
> 附近扎堆）当作**判据等价性**的证据 —— 而 `1e-4` 级的均值扰动**就足以跨过 1 对标对**，
> 所以「差 1 对」**既不能证明等价、也不能证明不等价，它对这个判断根本没有分辨率**。
> 能用的等价性证据只有 `average_correlation` 这类**连续量**（生产副本实跑 `0.4111`；
> `0.4113 / 0.5887 / 102` 是**同族值** —— 与 `0.4111 / 0.5889 / 101` 只差 `1e-4` 与 `1` 对，
> 属**边界差**，不是分歧）。
> **教训：用边界计数去判断两个口径是否等价，等于用一个不可微的指标做连续性判断。**

**与 (4) 的关系：方向一致，两档即 (4) 的落地形态。** (4) 要求「按有效基期算 gap」+「复制行内部收益记 `NaN`」；
两档把复制行判为非观测并让基期回退到最近真值观测，实测零收益观测 **107 → 6** ——
**残留的 6 条恰是那 6 条真值平价日**（`cur == prev` 且官方净值也逐位相同），
即复制行内部收益**已不再落成 `0.0` 混进矩阵**，**且真零没有被一起抹掉**。
（`B2` 时期的「`107 → 0`」正是它「宁可错杀」的体现，见上表。）
区别仅在 gating 手段：两档直接用 copy 规则 void 掉该行，
而 (4) 原设想是让 gap 变大后由跨期闸门接住 —— 结果等价，两档少一次间接。

**实测效果（生产副本实跑：34 只持仓、`snapshot_date = 2026-09-15`、`matrix_dim = 33`）**：
`average_correlation` **0.3480 → 0.4111**、`diversification_score` **→ 0.5889**、
`high_correlation_pairs` **76 → 101**、零收益观测 **107 → 6**（**残留 6 条 = 上表那 6 条真值平价日**，
即抹掉 101 条伪零、**未误抹真零**）、`copy_flag_total = 111`、`affected_labels = 11`。

> **口径（必带）**：`days = 60`（**行数**）窗口、阈值 `|ρ| > 0.8`、入矩阵 33 只。
> `high_correlation_pairs` **给区间不给裸值** —— 实测锚点 `101`，但它对窗口长度与阈值都敏感
> （`days`：`55→102 / 58→100 / 59→101 / 60→102 / 61→104 / 62→104 / 65→99 / 70→100`；
> 阈值：`>0.790→110 / >0.795→107 / >0.800→102 / >0.805→99 / >0.810→95`，`days=60`）
> ⇒ 引用时写「`101`（附口径）」，不要只说「多了 25 对」。
> 相对稳、可当精确值引用的是 `average_correlation` 与 `diversification_score`。
> **同族值（别当分歧）**：`0.4113 / 0.5887 / 102` 是 **`B2`（`9de2800`）修订**的值，
> 与上面的 `0.4111 / 0.5889 / 101`（两级 = `05c7288` = `6434f05` = WORKTREE）**同族**，
> 差异全部落在 `1e-4`（连续量）与 **1 对**（边界整数）这两个量级上 —— 即上表那「跨 1 对边界」的现象，
> **不是两条口径算出不同结论**。引用时必须带 rev 或带口径。

**四条断言（全部 PASS）** —— 用来钉住上面这些数：

| # | 断言 | 结果 |
|---|---|---|
| 1 | 6 条**真值平价观测**被误 void = `0` 条 | **PASS** |
| 2 | `2026-09-15` 被 flag 的标的 = `11` 只 | **PASS** |
| 3 | **ETF 侧**被 flag = `0` | **PASS** |
| 4 | 6 月块被 void = **10 只 × 10 个日期** | **PASS** |

> **⚠️ 「验证脚本本身必须先被验证」（教训留档）**：这批断言的**第一版复核脚本给出过干净的假 PASS** ——
> 「6 条真值未被 void」显示 PASS、「`09-15` 被 flag `0` 只」。根因是判据被写成**等值比较**：
> `'2026-09-15' in ds`，而 `ds` 的元素形如 `'2026-09-15 Tier2 段长=2'` ——
> **真正的匹配被 `[:6]` 的打印截断藏在了列表尾部**，于是「没比上」被读成了「没命中」。
> ⇒ **一个字符串比较写错，就能让「未验证」看起来像「已验证」，而且方向恰好是往好的那边报。**
> 复核任何「断言 PASS」时，先确认它是**真的命中了**、而不是**根本没比上**。

**适用范围风险（待办）**：两档里**只有 `Tier2`** 用 `is_otc_fund` 的白名单 `OTC_FUND_CODES`
（`Tier1` 不加 gate）——
- `Tier2` 侧的风险不变：**新增场外标的若未同步登记，横截面广度对它就静默不生效**
  （长复制段仍由 `Tier1` 兜住，但「只有 2 行的全市场事件」会漏）；
- `Tier1` 侧是**反向**风险：无 gate ⇒ 将来 ETF 侧若真的出现 `>=5` 的恒定价段，会被一并 void。
  当前实测该情形**不存在**（全历史 0 段），属**将来风险**而非现况。

建议补一条守卫：**「快照有行但不在白 / 黑名单」须显式告警**，不要静默放行。

---

## 问题十二：份额折算（拆分 / 合并）在快照侧**未同步入份额** —— 全历史 13 例、三族

> **来源与证据链**：本节的实跑证据来自 `fix-adj-fallback` 的 **#86 清单（当轮实跑）**及其脚本产物
> （`_exh.log` / `_ledger.log` / `_ledger2.log` / `_qty.log` / `_cost.log` / `_ret1d.log` / `_scope86*.log` / `_disc.log` / `_name.log`
> —— 这些产物在**接手工作目录**里，**未入库**，只作溯源用）；
> 我做了**算术自洽性核对**与口径复述，**未重跑其脚本**。
> 每条数字后面都带它所属的**口径**（扫描范围 / 判据 / 窗口 / 列集），**引用时请连口径一起引**。

### 一、为什么会出现在「已知数据问题」里

`portfolio_snapshots` 的 `quantity` 与 `current_price` 必须**同时**折算：拆分时价格减半、份额翻倍。
实测全历史 **13 处价格台阶**里，**5 处只折算价、没折算份额** ⇒ `market_value = quantity × current_price`
被**整倍数放大**，并把放大倍数**当成单日收益**喂给下游。影响是**两条独立的路径**：

1. **快照侧（真污染）**：`market_value` 本身错了 ⇒ 任何按 `market_value` 或 `current_price × quantity`
   取数的下游（组合市值、权重、特征表）都吃到伪收益。这条**只由 A 族（5 例）触发**。
2. **公式侧（与快照是否入账无关）**：`daily_return` 的分子取「**当日价 × 前日数量**」，
   在**真拆分日**（价减半 + 份额翻倍）这两个因子分别取自拆分前后两侧 ⇒ 比值必然 ≈ `1/k`
   ⇒ **必产伪 `-50%` 级暴跌**。这条**B 族（2 例，快照完全正确）也会触发** —— 见 §五。

⇒ **两条路径不能合并成一条修**：修好快照侧的折算，公式侧的伪跌**依然存在**。

### 二、枚举口径（引用任何「13 例」都必须带这四条）

| 项 | 取值 |
|---|---|
| 扫描范围 | `portfolio_snapshots` **全历史**（不限窗口、不设起始日、不设标的白名单） |
| 台阶判据 | **同一 `code` 相邻两行**的 `current_price` 比值 `r`，取 `|ln r| >= 0.20` |
| 同现要求 | **无** —— 逐 `(code, date)` 独立判定，不要求横截面同日出现 |
| `gap` 复核 | 命中后另算相邻快照自然日间隔 `gap`，用于**分族**（不是判据，见 §四） |

⇒ 命中 **13 处 / 13 只标的**（**每只恰好 1 处**）。年份分布：`2015×1`、`2021×1`、`2022×1`、`2024×2`、`2025×2`、`2026×6`。

> **为什么门槛取 `|ln r| >= 0.20`**：命中集里 `|ln r|` **最小**的一处是 `001765` 的 `r = 0.7461`（`|ln r| = 0.2929`），
> 门槛 `0.20` 相对它只有约 **`1.46×`** 余量 ⇒ 这个门槛是**有分辨力的**，不是"随手取的宽松值"。
>
> > ⚠️ **别把「`r` 的最小值」当成「`|ln r|` 的最小值」**（我第一版就写错了）：
> > `r` 最小的 `512010`（`0.2608`）其 `|ln r| = 1.344`，反而接近**最大** ——
> > 因为 `|ln r|` 对 `r` 与 `1/r` **对称**。13 例的 `|ln r|` 跨 `0.2929 ~ 1.3440`
> > （下界 `001765 0.2929`、上界 `512010 1.3440`）。
>
> 用 `|ln r|` 而不是「涨跌幅百分数」，正是为了对**上下行对称** ——
> `512010`（`r < 1`）与 `512810`（`r > 1`）分列两侧，用百分数记会把它们记成"不同量级"。

### 三、三族表（判别器：`quantity` 是否**反向补偿** `market_value`）

对每处台阶取 `prev` / `date` 两行，算两个比值：

```
qty_ratio = quantity(date)     / quantity(prev)        # 份额是否被同步折算
mv_ratio  = market_value(date) / market_value(prev)    # 市值是否被整倍放大
```

**判别规则**（`qty_ratio` **只在命中集内部作判别量**，**不是**发现判据 —— 见 §四末的说明）：

| 族 | 条件 | 例数 | 定性 |
|---|---|---|---|
| **A · 未入份额（伪收益）** | `qty_ratio == 1.0` 且 `mv_ratio == r` | **5** | `market_value` 被按 `r` 整倍放大 ⇒ **真污染** |
| **B · 已入账（市值连续）** | `qty_ratio ≈ 1/r`（实测 `2.0`）、`mv_ratio ≈ 1`（实测 `1.0159` / `1.0113`） | **2** | 份额已正确折算 ⇒ 快照无伪收益，**但公式侧仍失真**（§五） |
| **C · 其他（场外月末‑次月末）** | `qty_ratio 1.058 ~ 1.262`、`mv_ratio 0.72 ~ 0.91`、`gap = 31` | **6** | 性质**未定**，见 §七 未闭合 1 |

> **三个比值里只有一个独立信息 —— `qty_ratio`。** 因为 `market_value = quantity × current_price` 恒成立
> ⇒ **`mv_ratio ≡ qty_ratio × r`**（13 例逐例验算全部成立，如 `512810`：`2.0 × 0.5079 = 1.0158 ≈ 1.0159`）。
> 所以：
> - A 族的 `mv_ratio == r` **只是 `qty_ratio == 1.0` 的改写**，不是第二条独立证据；
> - B 族的 `mv_ratio ≈ 1` **等价于 `qty_ratio ≈ 1/r`**（份额恰好补回价格倍数）；
> - **判别器只需读 `qty_ratio` 一个数**：`1.0` ⇒ A 族、`≈ 1/r` ⇒ B 族、**两者之间** ⇒ C 族。
> 这一条也是「不能用 `mv_ratio` 单独判定」的原因 —— 它是因变量，不是自变量。

#### A 族 5 例（逐例实测值）

| 代码 | `prev` | `date` | `snap_pre` | `snap_post` | `r` | `qty_ratio` | `mv_ratio` | 伪收益 `r-1` | 同期 `qfq` 价比 |
|---|---|---|---|---|---|---|---|---|---|
| `510500` | `2015-04-10` | `2015-04-15` | `2.2430` | `7.8180` | `3.4855` | `1.0000` | `3.4855` | **`+248.55%`** | **无数据**（行情表自 `2018-01-02` 起） |
| `512010` | `2021-06-24` | `2021-06-28` | `3.2060` | `0.8360` | `0.2608` | `1.0000` | `0.2608` | **`-73.92%`** | `0.802 → 0.836` = **`+4.2%`** |
| `512100` | `2022-09-01` | `2022-09-05` | `0.9820` | `2.7130` | `2.7627` | `1.0000` | `2.7627` | **`+176.27%`** | `2.608 → 2.635` = `+1.0%` |
| `159300` | `2024-06-24` | `2024-06-25` | `0.9730` | `3.4620` | `3.5581` | `1.0000` | `3.5581` | **`+255.81%`** | `3.489 → 3.462` = `-0.8%` |
| `516160` | `2024-09-13` | `2024-09-18` | `0.4970` | `1.5970` | `3.2133` | `1.0000` | `3.2133` | **`+221.33%`** | `1.608 → 1.597` = `-0.7%` |

⇒ **两个结论必须一起记**：

1. **`qfq` 价比全部落在 `±1.5%` 内** ⇒ **行情表（`etf_price_history`）是干净的**，
   脏的**只在快照侧的基准**。所以「有折算」不等于「行情表要清洗」。
2. **方向逐例不同**（4 例 `r > 1`、`512010` 一例 `r < 1`、`510500` 无复权数据可判）
   ⇒ **不能用「折算一律把市值放大」来记忆** —— `512010` 是缩小到 `0.26` 倍。

#### A 族的旁证（**不依赖复权价**的独立核验，四条）

| 旁证 | 实测 |
|---|---|
| **`cost_price` 在台阶日逐位不变** | `159300` `2024-06-25`：价 `×3.5581`、`cost_price ×1.0000`、`pnl_rate -52.90% → +67.57%`、`pnl -2186 → +2792`；`512100` / `516160` / `510500` 同形 |
| **`trade_records` 无对应记录** | `159300` / `516160` 台阶日**前后各 20 天** `trade_records` 命中 **`0` 行** ⇒ 不是真实买卖 |
| **`name` 无变化** | 4 例全历史 `name` 只在最早的 5 行是短名（`300ETF` / `1000ETF` / `新能源` / `500ETF`），其后恒定（`沪深300ETF富国` 等）⇒ 不是份额类别变更 |
| **`q_ratio` 全窗恒 `1.0`** | 台阶日**前后各 6 行**逐行核对：`q_ratio ≡ 1.0`、`q_ratio × p_ratio ≡ mv_ratio` ⇒ 该标的这一整段**份额从未变动**，台阶**完全是价造成的** |

> ⚠️ **`portfolio_snapshots` 没有 `volume` 列**（列集只有 `id` / `date` / `code` / `name` / `quantity` /
> `cost_price` / `current_price` / `market_value` / `pnl` / `pnl_rate` / `ytd_return` / `beta`），
> ⇒ **无法用成交量这条最有说服力的旁证**，只能用上面四条间接证据。

#### B 族 2 例

| 代码 | `prev` | `date` | 价 `prev → date` | `quantity prev → date` | `qty_ratio` | `mv_ratio` | `gap` |
|---|---|---|---|---|---|---|---|
| `512810` | `2025-06-20` | `2025-06-23` | `1.197 → 0.608`（`×0.5079`） | `25000 → 50000` | `2.0` | `1.0159` | `3` 天 |
| `159220` | `2025-11-07` | `2025-11-10` | `1.240 → 0.627`（`×0.5056`） | `10000 → 20000` | `2.0` | `1.0113` | `3` 天 |

⇒ 这两例 `quantity` 与 `current_price` **反向补偿**、`market_value` 连续（`+1.59%` / `+1.13%`）
⇒ **快照侧没有伪收益** ⇒ **它们不属于 A 族，不要在"13 例全是脏数据"的口径下被引用**（§五 单独处理）。

### 四、`gap` 的分布，以及它为什么**不能**当判据

| 族 | 相邻快照自然日间隔（逐例） |
|---|---|
| **A**（5 例） | `1`（`159300`）/ `4`（`512010`）/ `4`（`512100`）/ `5`（`510500`）/ `5`（`516160`） |
| **B**（2 例） | `3` / `3` |
| **C**（6 例） | **`31`**（场外 `2026-06-30 → 07-31`） |

⇒ **`gap` 只对 C 族有分辨力**：`31 > 12` 把 C 族干净分出去；而 A / B 族的 `gap ∈ {1,3,4,5}`，
与「普通相邻交易日」**完全同形**（`159300` 的 `gap` 就是 `1`）。
⇒ **`gap` 是复核项 / 分族项，不是发现项**；把 `gap` 当判据会**一条都发现不了**。

同理，`qty_ratio` **也**不能当发现判据 —— 它在 A 族（`1.0`）与 C 族（`1.06 ~ 1.26`）**都不是极端值**，
只有把范围先收到「价格台阶」里，它才有分辨力。

### 五、公式侧：**真拆分日必产伪 `-50%`**，与快照是否入账无关

`daily_return`（`src/analysis/portfolio.py:503-541`）：
分子 `Σ_common(当日价 × 前日数量)`、分母 `Σ_common 前日市值`。
「`common` 里数量在分子分母两处约掉」这个直觉**只在没有份额变动的日子成立**；
真拆分日分子取的是**拆分后的价 × 拆分前的数量** ⇒ 单只贡献比值 ≈ `1/k`。

B 族两例是**公式侧最干净的证据**（快照侧完全正确、收益侧仍然错）：

| 日期 | 代码 | 价 `prev → date` | 数量 `prev → date` | 单只伪跌幅（手算） | 失真值 | 修正值 | 差额 |
|---|---|---|---|---|---|---|---|
| `2025-06-23` | `512810` | `1.197 → 0.608` | `25000 → 50000` | `1 − 0.608×25000 / 29925` = **`-49.2%`** | `-4.5286%` | `-2.1822%` | `+2.35pp` |
| `2025-11-10` | `159220` | `1.240 → 0.627` | `10000 → 20000` | `1 − 0.627×10000 / 12400` = **`-49.4%`** | `-1.5150%` | `-0.6463%` | `+0.87pp` |

> **口径（必带）**：全历史 `daily_return` 序列**偏差最大的两处**（第 1、2 名）；
> 「失真值」= 现产线读数，「修正值」= 把该标的当日分子换成「前日价 × 前日数量」后的读数。
> **自洽性核对**（可当场验算）：`0.492 × w = 2.35pp ⇒ w ≈ 4.8%`、`0.494 × w = 0.87pp ⇒ w ≈ 1.8%`
> —— 即两例当日在前日市值里的**隐性权重**，与这两只当时的小仓位量级相符。
> ⇒ 数值互相自洽；但**这两个数我未重跑产线序列复核**，引用时按「`fix-adj-fallback` #86 实跑」记。

**`510500` 的观察 —— 闸门侧的系统性死角**：实时管线的折算闸门 `_conversion_qfq_ratio`
（`CONVERSION_SUSPECT_RATIO = 0.25`，即 `r ∉ [0.75, 1.25]` 时触发）兜底靠 `etf_price_history` 取复权价比，
而 `510500` 的行情表**自 `2018-01-02` 起**、`portfolio_snapshots` 却从 `2015-03-06` 起
⇒ 对 2015 年的这处台阶，闸门**必然走「无可用复权价…已剔除」分支**
—— **不是阈值失效，是根本没有基准数据可比**。
⇒ **闸门的覆盖面 = 行情表覆盖面 ∩ 快照覆盖面**：行情表起点晚于快照起点的标的，
在**重叠区之前**存在**系统性盲区**（`512010` 自身行情表起点也晚于其快照，同类）。

> 顺带定死一条：`CONVERSION_SUSPECT_RATIO = 0.25` **只存在于实时管线**，
> **两个回填脚本都没有这道闸** ⇒ 13 例**全部落在闸门管不到的历史段** ⇒
> **闸门无法回溯修补**，只能靠一次性回填或在下游按 §三 的判别器屏蔽（见 §七 未闭合 5）。

### 六、二次污染：伪收益已经**双向落盘**

伪收益不只在内存里，它**写进了特征表**：`etf_features.ret_1d` 与库内精确 `r - 1` **逐位相等**（`|差| = 0.000e+00`）：

| 代码 | 日期 | 库内精确 `r` | `r - 1` | `etf_features.ret_1d` |
|---|---|---|---|---|
| `510500` | `2015-04-15` | `3.4855104770` | `+2.4855104770` | `+2.4855104770` |
| `512010` | `2021-06-28` | `0.2607610730` | `-0.7392389270` | `-0.7392389270` |
| `512100` | `2022-09-05` | `2.7627291242` | `+1.7627291242` | `+1.7627291242` |
| `159300` | `2024-06-25` | `3.5580678314` | `+2.5580678314` | `+2.5580678314` |
| `516160` | `2024-09-18` | `3.2132796781` | `+2.2132796781` | `+2.2132796781` |

> **⚠️ 这里有一条我自己的精度假警报，留档**：第一版复核脚本用**四舍五入到 4 位**的 `r` 去比
> `ret_1d`，阈值 `1e-6` ⇒ 全部报「不一致」。改用**库内精确 `r`** 后 `|差| = 0`。
> ⇒ **比对落盘值时必须用库内原始精度取数**，不要从打印过的小数反推。

**污染面（越界计数，`|·| > 1`）—— 引用必须同时带「窗口」与「列集」**：

| 项 | 取值 |
|---|---|
| **窗口** | 案发日**起**、向后取 **`26` 个交易日**（本节表口径） |
| **列集（窄）** | 剔 `kdj_k` / `kdj_d` / `kdj_j` / `atr_pct` / `parkinson_vol_20d` / `hl_range_20d` / `volume_zscore_20d`（OHLC 族）与 `ff_*` / `hs300_*`（非价格源）⇒ **`23` 列**（含 `rsi_14`） |

| 代码 | 案发日 | 含案发日 | 不含案发日 |
|---|---|---|---|
| `510500` | `2015-04-15` | `153` | `139` |
| `512010` | `2021-06-28` | `101` | `95` |
| `512100` | `2022-09-05` | `116` | `104` |
| `159300` | `2024-06-25` | `91` | `80` |
| `516160` | `2024-09-18` | `136` | `124` |
| **合计** | — | **`597`** | **`542`** |

> **⚠️ `597` / `542` 这两个数不要当定稿引用（2026-09-16 收到对账异议，未收敛）：**
> - **本表的列集写的是「窄口径 `23` 列，含 `rsi_14`」—— 而本表上方自己又写了「`rsi_14` 等有界列**理应剔**」**，
>   两者**互相矛盾**：带 `rsi_14` 会把总数抬高约 `+130`（它是 `0~100` 的天然越界列，对 `|·| > 1` 无判别力）。
>   ⇒ **本表当前形式（含 `rsi_14`）不应作为主口径。**
> - **同一批数据另有两版读数**（`19` 列版 = `401`；剔 `rsi_14` 版），我**尚未与来源方收敛**，
>   且 `fix-adj-fallback` 报的 `586` 我**尚未复现出**（与我的 `597` 差 `11`）。
> - ⇒ **在 `11_measurement_conventions.md` 把「主口径 / 上下界 / 不可引用」三档定死之前，
>   本表只作为"折算污染面很宽"的定性证据，不要引用具体数字。**
>   定性结论不受影响：五个案发日的 close 族越界计数都在**几十到一百多**量级（远超正常日）。

> **⚠️ 列数口径更正（`32 → 30`）**：本节此前草稿按 **`32` 列**统计 —— **错**。
> 正确口径是 **`30` 列**（= `40` 总列 − `date`/`code`/`feat_version` − `kdj_k`/`kdj_d`/`kdj_j`
> − `atr_pct` − `parkinson_vol_20d` − `hl_range_20d` − `volume_zscore_20d`）；
> 草稿漏剔了其中的两列。⇒ **引用这类计数必须贴列清单**，只写「`N` 列越界」无法复现。
>
> **⚠️ 窗口口径必须写明"几行"**：我的一版用 **`27` 行**、`verify-p1-batch` 的一版用 **`26` 行**，
> 两版在**同一列集**上差 `[3, 0, 1, 1, 1]`（次序 `510500` / `512010` / `512100` / `159300` / `516160`）；
> **把窗口收到 `LIMIT 26` 即与对方逐项相等**。⇒ 差 1 行就足以让「双方各说各的数」，**窗口行数属必带口径**。
>
> 另有一版更窄的列集（在上面的基础上**再剔 `rsi_14` 等有界列**）同样**双方逐项一致**
> （`rsi_14` 全表取值域 `0.00 ~ 98.88`，对 `|·| > 1` 判据天然无意义，理应剔）。
> **门槛不在"哪些标的"上**：`etf_features` 的 `code` 全集 = **`22` 只**，与 `resolve_target_codes()` 输出**逐项一致**。

### 七、未闭合项（**闭合前不得当成"已定性"**）

1. **C 族 6 例性质未定**：场外 `2026-06-30 → 07-31`（`001323` / `001407` / `001437` / `001765` / `166301` / `519770`），
   `qty_ratio ≈ 1.058 ~ 1.262`、`mv_ratio ≈ 0.72 ~ 0.91`、`gap = 31`。
   `qty_ratio` **既不像 A 族（`1.0`）也不像 B 族（`2.0`）** —— 可能是场外的**份额确认 / 分红再投**，
   也可能只是**月末估值口径切换**。**未取得场外净值侧证据前，C 族不得并入 A 族统计**（合并会把 5 例说成 11 例）。
2. **`512810` / `159220` 的 `ret_1d` 是否也落了伪值**：`etf_features` 未收录这两只的对应行；
   但 §五 已证明**公式侧**在这两天失真 ⇒ **「表里没有」不等于「没落盘」**，需另查报告 / 前向收益侧产物。
3. **闸门「剔除」与「记 `NaN`」的语义差异未验证**：走「无可用复权价」分支时该行是**整行剔除**，
   而不是把该点收益记 `NaN` —— 本例里这个行为**恰好**起了作用，但它对下游（窗口长度、`overlap_days`）的语义不同。
4. **`512010` 是否存在**第二处台阶：复核脚本曾把它的 `date` 报成 `2016-01-04`（前一行为 `2015-12-31`、`gap = 4`），
   与 §三 的 `2021-06-28` **是两次不同事件、还是一次误采，未查清**
   ⇒ 若成立，**全历史例数应为 `14` 而非 `13`**。这是本节唯一一处**计数不确定**。
5. **回填侧无闸门（修法未定）**：`CONVERSION_SUSPECT_RATIO = 0.25` 只在实时管线；
   13 例全在历史段 ⇒ 需要**一次性回填**或**下游屏蔽**二选一，**尚未排期**。
   注意 §五 的结论：**回填修好快照侧，公式侧仍错** ⇒ 两件事要分别排。

### 八、`#91` 收口：`daily_return` 的 `common`（共同持仓法）口径 = **定义域差异，不是 bug**

**收口结论（一句话）**：`#91`（「`daily_return` 与市值台阶不一致，四例中两例对不上」）
的成因是 **`common` 的定义域差异**；`daily_return` 采用**共同持仓法**是**有意为之、且是正确的**，
**不按缺陷处理**。本条**结案**（结论来源 `#103`；本文只做登记与消歧，不重跑它的数）。

**口径（必带）**
- 位置 —— **本节只保留一套行号：`HEAD` 系**（原文为唯一锚点，行号仅作导航；格式见 `11_*` §4 约定 9）。
  **取证版本**：`HEAD = 43d06ce`（2026-09-17 取证，`git show HEAD:src/analysis/portfolio.py`）。
  ⚠️ **本节旧版曾并列「`HEAD` / 工作区（在盘）」两套行号，该列已整体删除** ——
  `#115`（`7ee4cf5`）/ `#116`（`10d3ec2`）已提交、且 `#115` 同 commit 还带入了**口径 B**，
  工作区与 `HEAD` 已**逐字一致**，「第三套行号」不再存在；同时旧版写的「查 `HEAD` 就把这些数整体 `−20`」
  **已整条删除**（理由见本节末「行号会漂」段的作废记录）。**引用本节一律以原文锚定位，不要按行号跨提交平移。**

  | 锚点（可 `grep` 的原文，`HEAD` `43d06ce` 实测命中） | `HEAD` |
  |---|---|
  | `SELECT date FROM portfolio_summary WHERE date < ? ORDER BY date DESC LIMIT 1` | `:818` |
  | `prev_dt = prev_row[0]` | `:823` |
  | `"SELECT code, quantity, market_value FROM portfolio_snapshots WHERE date = ?",` | `:832` |
  | `comparable_codes = set(curr_codes.keys()) & set(prev_snapshots.keys())` | `:843` |
  | `common_codes = comparable_codes & fresh_codes` | `:846` |
  | `if prev_common_mv > 0:` | `:904` |
  | `daily_return = daily_pnl / prev_common_mv * 100` | `:906` |
  | `def _conversion_qfq_ratio(self, cur, code: str, prev_dt: str):` | `:664` |

  > ⚠️ **表内两处须特别注意（旧版锚点已被 `#115` 改名，属「锚本身变了」而不是「行号漂了」）**：
  > - 旧版的 `common_codes = set(curr_codes.keys()) & set(prev_snapshots.keys())` 在 `HEAD` 里**已经不是这个变量名** ——
  >   同一句右值现在赋给 **`comparable_codes`**（`HEAD :843`），而 `common_codes` 改为在**下一层的收窄**（`:846`）。**旧文本已 `grep` 不到，不得再引。**
  > - 二者是**两个不同的量**：`comparable_codes` = 当日持仓 ∩ 前日快照（**可比**）；`common_codes` = **可比 ∩ 当日价新鲜**。
  >   本节下文凡说「`common` 口径」，`HEAD` 之后请指明是**哪一层**。
  ⇒ 本节其余行号**已全部按 `HEAD` `43d06ce` 重取**（逐条 `grep` 证明见下方各段的原文锚）。

  > 📌 **历史留档（已作废，仅用于解释旧版为什么错，不作为引用依据）**：
  > 本节旧版写过「`2026-09-16` 有一笔未提交改动 `+20/0`，属 `#116`：① `:21` 插 1 行 import；
  > ② `:267` 插 19 行闸门调用块」——这笔归因**本身是对的**（`1 + 19 = 20`），
  > 但随后 `#115` 在途改动把位移推到 `+234`，**一天内出现两个差值** ⇒ 任何「按上次差值做减法」的规则**结构上必然失效**，
  > 故该规则与「工作区」列一并删除。**不要把它们加回来。**

  ⇒ 分子 `Σ_common(当日价 × 前日数量)`、分母 `Σ_common 前日市值`。
- ⚠️ **分母是 `common` 持仓的前日市值之和，不是前日 `total_value`**（逐只累加，`HEAD` `43d06ce` 两处）：
  - `HEAD :900` —— `prev_common_mv += prev_mv`（**分支内**，紧随 `price_adj_mv += prev_price * qfq_ratio * prev_qty`）
  - `HEAD :903` —— `prev_common_mv += prev_mv`（**无条件**，紧随 `price_adj_mv += curr_price * prev_qty`）
  > ⚠️ **`prev_common_mv += prev_mv` 这一行在 `HEAD` 里出现两次（`:900` / `:903`），不唯一 ⇒ 不得单独作锚**，
  > 必须带上邻行（如上两条各自的「紧随行」）构成组合锚，否则 `grep` 命中的是哪一处无法判定。
  ⇒ 当日新增 / 已清仓的标的**不进分母**。
- 覆盖面 **`96.4% / 3.6%`** —— **转记 `#103`**，且**它是「行覆盖」口径、不是「市值覆盖」**
  （`verify-p1-batch` 2026-09-16 明确：他手上**没有**市值口径的数）。
  ⚠️ 我这里**不重跑、不改写它的分母**；引用时必须回 `#103` 取分母，
  **不要只引 `96.4%` 这个裸数，更不要把它读成「市值口径覆盖了 96.4%」**。
  🔴 **取证标记（必须随数一起引，2026-09-17 裁定）**：
  > `96.4% / 3.6%（取证版本：#115 之前的 universe；口径 = 行覆盖，分子/分母定义见 #103；当前口径下未重算）`
  ⇒ 「**当前口径下未重算**」是**事实陈述**（可核：`#103` 测于 09-16、`#115` 落于 09-17），**不是因果断言**。
  ⛔ **本节不得写任何「因为 `#115`，所以 `96.4%` 已过期」一类的原因** ——
  `96.4%` 的**分子/分母定义不在本节手上**，定义未知时「该数是否仍成立」**方向都可能相反**（若分子分母同步扩张，比值可能几乎不变）。
  ⇒ 纪律同 `11_*` §4 / `12_*` §17.4：**能逐行读到的代码事实可以写；涉及未知定义的因果一律不写。**
  该数是否需要重测，已转派 `diag-otc-coverage` 做「可复现性判定」（先答分子/分母/样本/日期/脚本，再决定换数还是删数）。
- 「对不上」的那部分 = **`common` 之外的持仓**（当日新增 / 已清仓 / 前日快照缺行，
  **以及 (v) 落地后的「当日价非当日」**），
  它们**按定义就不该进当日日收益** ⇒ 对不上是**口径使然**，不是算错，也**正是那 `3.6%` 残余面的来源**
  （⚠️ `3.6%` 的取证标记同上：测于 `#115` 之前的 universe，**当前口径下未重算**）。

  > 🔎 **2026-09-19 复核（团队 lead 独立重测，撤回 `diag-otc-coverage` 的「删数」误判）**：
  > `diag-otc-coverage` 的「删数」结论建立在一个**分母混用**上——把 **OTC 口径**的 `96.4%` 拿去和**全量** `99.76%` 对比，
  > 得出「~15× 差、不可复现」。我以**只读 SQL** 独立重测（逻辑：`p2_verify_coverage.py`；
  > 行覆盖定义 = 每只持仓行是否在前一交易日（全局快照日期集的上一日）也有同 `code` 行）：
  > - **全量**（35 码，36,275 行）行覆盖 = **99.7574% / 缺口 0.2426%**；
  > - **OTC 仅**（14 只场外码：001194/001323/001407/001437/001765/002152/007994/008269/027293/100032/159732/166301/519770/880013，1,787 行）行覆盖 = **96.5305% / 缺口 3.4695%**；
  > - OTC 窗口 `2026-01-31~2026-09-14` 严格缺口（前一日须同在该窗口快照内）= **9.37%**。
  > **结论**：`96.4%/3.6%` 作为**行覆盖口径是可复现的**——当前 universe 下 OTC 可比层 = 96.53%（与文档 `96.4%` 仅差 ~0.1pp），
  > 全量可比层 = 99.76%；而 `#103` 的 `common` = 可比 ∩ 当日价新鲜（比「可比层」更窄），落点 ~96.4% 与之自洽。
  > ⇒ **撤回「删数」裁决，改判「保留 + 补当前 universe 验证数」**；原 `取证标记（当前口径下未重算）` **维持不变**，引用仍须回 `#103` 取分母。
  > ⛔ 本段**不写任何「因为 `#115`/`#115` 所以 `96.4%` 过期」的因果**（纪律同 1924）；仅补「可比层」兄弟数，不得替换文档原数。

**⚠️ 不得跨口径引用（本条真正的实操价值在这一句）**

`daily_return` 是**共同持仓法**，而 `Σmarket_value` / `total_value` 是**全持仓法**。
两者**恒等只在「当日与前日持仓集合完全相同」时才成立**。
⇒ 拿 `daily_return` 去对 `Σmarket_value` 的日变化（或反向），**在任何持仓集合发生变化的日期都会对不上** ——
**这不构成新缺陷，不要重新开票**。做这类对账**必须先声明用的是哪个口径**（分母是什么），否则必然对出一堆假差异。

**⚠️ 五条边界限定（(i)~(iii) 是口径；(iv) 是码内缺陷（已部分收口）；(v) 是 2026-09-17 新落地的「口径 B」收窄）**

> 行号版本：`HEAD` `43d06ce`（2026-09-17 取证）。原文为锚，行号仅作导航。

| # | 限定 | 后果 |
|---|---|---|
| (i) | 基期**不是**「上一交易日」，而是「**上一条存在的 `portfolio_summary` 行**」 | 缺行 ⇒ 基期被推远 ⇒ `daily_return` **静默变成多日收益**；这是 `#116` 里「两日收益且符号相反」的**唯一**机制，**不需要第二个假设** |
| (ii) | 分母是 **`common` 持仓的前日市值之和**，不是前日 `total_value` | 当日新增 / 清仓**不贡献分母** ⇒ `daily_return` 与 `Δtotal_value / total_value_{t−1}` **在 `common` 集合变化日必然不等**（`HEAD` 之后请指明 `common` 是「可比」层还是「可比 ∩ 新鲜」层，见 (v)） |
| (iii) | **折算闸门的两个 `continue` 分支对分母的影响不同**（见下，必须拆开写） | 折算日的 `daily_return` 与任何「全持仓口径」**不可比**，必须标注 |
| (iv) | **还有一个 `fallback` 分母：全持仓 `prev_value`**（见下「第三个分母」） | ⇒ `daily_return` **一共有三个分母**。⚠️ **2026-09-17 已部分收口**：触发哨兵由 `daily_return == 0` 改为**独立布尔** `daily_return_computed`，且该支**现在必留痕**（日志 + `alerts` 表）—— 见下「(iv)」段 |
| (v) | 🔴 **口径 B 收窄（2026-09-17 落地，`#115` `7ee4cf5` 同 commit）**：`common` = **可比 ∩ 当日价新鲜** | 当日价非当日的标的（典型：T+1 披露的场外基金）**既不进分子也不进分母** ⇒ `daily_return` 的**覆盖度 < 100%**，**不得**再与全持仓 `total_value` 跨口径相乘；见 (v) 段 |

**(iii) 必须拆成两支，否则会把一个正确的事写错**（`HEAD` `43d06ce`）：

- **查不到复权价支**（`HEAD :886` `if qfq_ratio is None:`，紧随 `HEAD :891` 文案含 `已从日收益计算中剔除`）
  ⇒ **双边剔除**：该标的**既不进分子也不进分母** ⇒ **分母收缩**。
- **改用复权价比支**（`HEAD :896` 文案含 `改用复权价比`）⇒ **分母照计**
  （`HEAD :900` 已 `prev_common_mv += prev_mv`），
  只是把分子换成 `prev_price * qfq_ratio * prev_qty`（`HEAD :899`）⇒ **分母不收缩**。
- 两支的**共同入口**：`HEAD :883` `if abs(raw_ratio - 1) > CONVERSION_SUSPECT_RATIO:`
  （紧随 `HEAD :884` `guard_fired = True`）。

> ⚠️ **旧版此处引的 `:541-549` / `:549` / `:556` / `:554` / `:555` 已全部作废**（那是 `HEAD 0d0b3b9` 系的行号，现已重取如上）。
> ⛔ **旧版把该支称作「罕见」的表述已被删除** —— 依据是**代码自述**：`HEAD :887` 的注释里写着
> 「查不到可用复权价（**典型：场外基金 / 行情陈旧**）」⇒ 该支的典型触发对象**恰是场外基金**，
> **不得**按「罕见」表述（本节不写对频次方向的预测，只引这句代码自述）。

**改用复权价比支为什么是「正解」而不是「另一种剔除」**（两条理由，`verify-p1-batch` 2026-09-16 补，我已核过代数）：
1. **它是防重复累加的 `continue`，不是剔除**：`HEAD :902`（`price_adj_mv += curr_price * prev_qty`）
   与 `HEAD :903`（`prev_common_mv += prev_mv`）是**无条件累加**；
   若没有该支的 `continue`（`HEAD :901`），走复权价比的标的会被**累加两次** `prev_mv`。
2. **该支每只标的的净贡献恰为 `qfq_ratio`**：
   `(prev_price * qfq_ratio * prev_qty) / prev_mv`，而 `prev_mv = prev_price * prev_qty` ⇒ 数量与价格**全部约掉**，只剩 `qfq_ratio`。
   ⇒ 语义 = **纯复权价比收益**，口径最干净 ⇒ **查不到复权价支的收缩才是副作用**，该支不是折中。

**（iii）收窄后的生存范围（照此引用，不要写「两支都双边剔除」）**：
> 「折算日分母被收窄」**仅在该日存在走「查不到复权价」支的标的时成立**。
> 该日日收益分母 = 「当日 `common` 篮子 − 被该支剔除的标的」，
> 故该日 `daily_return` **与总市值口径不可比**。
> 若该日所有超阈标的都走「改用复权价比」支，分母仍是**当日 `common` 篮子**（只是分子换成复权价比）。

**⚠️ 日志判据：折算闸门两支可从盘上产物反推（不必读源码）**

| 告警文案（**运行期字符串**，作锚，不随行号失效） | 走向 | 对分母 |
|---|---|---|
| 含 **`已从日收益计算中剔除`** | 查不到复权价支（`HEAD :891`） | **收缩** |
| 含 **`改用复权价比`** | 改用复权价比支（`HEAD :896`） | **不收缩** |
| 含 **`[口径B] daily_return 排除`** | (v) 口径 B 收窄（`HEAD :862`，`logger.warning(` 在 `:861`，块 `:861-868`） | **缩小篮子**（不属折算闸门，见 (v)） |

> ⚠️ 旧版此表内引的 `:544-548` / `:550-553` **区间已作废**；**区间一律不再写**，只留**可 `grep` 的文案**
> —— 文案是运行期字符串，与源码行号解耦，这是本表能长期使用的原因。

**(v) 口径 B 收窄：`common` 现在是「可比 ∩ 当日价新鲜」（2026-09-17 落地，随 `#115` `7ee4cf5`）**

**逐行读到的代码事实**（`HEAD` `43d06ce`，2026-09-17 取证）：

- `HEAD :843` —— `comparable_codes = set(curr_codes.keys()) & set(prev_snapshots.keys())`（**可比**层：当日持仓 ∩ 前日快照）
- `HEAD :774` —— `def _resolve_fresh_codes():`（**新鲜**层；其三档退化顺序见该函数 docstring）
- `HEAD :844` —— `fresh_codes, freshness_source = _resolve_fresh_codes()`
- `HEAD :845` —— `excluded_stale = sorted(comparable_codes - fresh_codes)`
- `HEAD :846` —— `common_codes = comparable_codes & fresh_codes`（**最终篮子 = 可比 ∩ 新鲜**）
- `HEAD :847` / `:848` —— `prev_value_comparable`（可比层前日市值）与 `prev_value_included`（纳入层前日市值）**是两个量，分别计算**
- `HEAD :861-868` —— 存在被排除标的时**必留痕**（`[口径B] daily_return 排除 %d 只价格非当日的标的…`）

⇒ **对本节的直接影响（三条）**：
1. 上表 (ii) 说的「`common` 前日市值和」**现在有两个候选层**（可比 / 可比 ∩ 新鲜）⇒ **引用必须指明是哪一层**，否则同一个词指两个量。
2. 折算闸门两支是在**收窄之后**的篮子上跑的 ⇒ (iii) 的「分母收缩」是**在此基础上的二次收缩**，两层不可合并叙述。
3. **覆盖度不是 100%** ⇒ `daily_return` **不得**与全持仓 `total_value` 跨口径相乘；覆盖度的口径与实测见 `12_*` §17.2，本节的 (v) 段**不复制数字**。

> 🔴 **覆盖度数字一律不得硬编码**：`daily_return_coverage` 是**运行期键、不落库**
> （只读生产库实测 `portfolio_summary` 共 **16 列、无该列**，2026-09-17 `mode=ro`）。
> ⇒ 引用时**必须**标为「**运行时值**」并给取证方式与日期，**以运行时算出的数为准**；
> **禁止**写成「报告里会印出 N 只 / X%」—— 邮件路径拿不到该数字（见 `12_*` §17.6 第 1 条）。

⇒ 与本节「可执行」的取向一致：**分母口径必须能从盘上产物反推**，不能只靠人读源码。

> **⚠️ 上面的日志判据表覆盖 (iii) 的两支与 (v) 的收窄。** (iv) 单列如下 ——
> 它**旧版**完全不可反推（无告警、无落盘），**现版已可反推**（日志 ＋ `alerts` 表）。

**(iv) 第三个分母：`fallback` 把分母从 `common` 换成全持仓 `prev_value`**

> 🔴 **2026-09-17 已部分收口**：触发哨兵由 `daily_return == 0` 改为**独立布尔** `daily_return_computed`，
> 且该支**现在必留痕（日志 + `alerts` 表）**。故下面**同时给「现版（收口态）」与「旧版（缺陷态）」两段锚点**，
> 引用时**必须说清是哪一版**，不要把旧版当当前行为。

**现版锚点（`HEAD` `43d06ce`，2026-09-17 取证；节选，整块为 `:913-938`）**：
```python
# fallback: 用 total_value 简单对比（仅当无前日快照数据时）
# 闸门介入时抑制：daily_return 可能恰好为 0，但 total_value 仍含被折算污染的
# 当日价格，走这条分支会把刚拦下的假收益原样算回来。
# 口径 B（2026-09-17）：判定哨兵由 `daily_return == 0` 改为独立布尔
# `daily_return_computed` —— 原写法把**真实平价日**（合法值 0.0）误判成
# 「未计算」，静默把分子分母换回全持仓口径，使 daily_return 序列变成
# 「多数日『当日新鲜』口径 + 个别日『全持仓』口径」的混合口径。
if not daily_return_computed and prev_value > 0 and not guard_fired:
    daily_pnl = total_value - prev_value
    daily_return = daily_pnl / prev_value * 100
    # 这是**口径切换**，必须显式留痕（日志 + alerts），不许静默。
    logger.warning(
        "[口径B] 无可用「当日新鲜」共同篮子（freshness_source=%s, 纳入=%s）："
        "daily_return 回退为全持仓口径 total_value/prev_value（%.2f），"
        "该值口径与其它交易日不同，报告须显式标注",
        coverage.get("freshness_source"), coverage.get("included_n"), daily_return
    )
    coverage["caliber"] = "total_value_fallback"
    record_error_alert(
        self.db.db_path,
        "daily_return_caliber_fallback",
        ...
    )
```
（哨兵初值：`HEAD :761` —— `daily_return_computed = False`。）

**旧版锚点（历史 / 缺陷态，已修复 —— 不要再引为「当前行为」）**：
```python
if daily_return == 0 and prev_value > 0 and not guard_fired:
    daily_pnl = total_value - prev_value
    daily_return = daily_pnl / prev_value * 100
```

⇒ **`daily_return` 的分母 / 篮子一共三项 + 一个收窄层**：

| 分母 / 篮子 | 何时使用 | 盘上可反推?（`HEAD` `43d06ce`） |
|---|---|---|
| `prev_common_mv`（当日 `common` 篮子） | 常规 | 间接（无相关告警即为常规） |
| 收缩后的 `common` | 当日有标的走「查不到复权价」支 | ✅ 日志文案 `已从日收益计算中剔除` |
| **（收窄层）** 可比 ∩ 新鲜 | 见 (v) | ✅ 日志文案 `[口径B] daily_return 排除` |
| **`prev_value`（全持仓）** | **`not daily_return_computed and prev_value > 0 and not guard_fired`** | ✅ **现在可反推**：日志文案 `无可用「当日新鲜」共同篮子` ＋ **`alerts` 表 `rule_name='daily_return_caliber_fallback'`（`level='error'`）** |

- **`alerts` 落库已逐行核实**：`record_error_alert` 的实现是
  `INSERT INTO alerts (rule_name, level, message, created_at, acknowledged) VALUES (?,?,?,?,0)`，
  `level` 硬编码 `"error"`（`src/analysis/snapshot_gate.py:135-150`，`HEAD` `43d06ce`）。
- ⚠️ **但 `portfolio_summary` 行本身仍不带口径标记**（只读生产库实测 16 列、无该列，2026-09-17）
  ⇒ 按**行值**反推仍不可执行，必须**跨表查 `alerts`**。⇒ 下面「三件套」表的第 3 项因此只是**部分闭合**。
- ⚠️ **不得据此反推「历史上从未走过 `fallback`」**：`record_error_alert` 是 `#116`（`10d3ec2`，2026-09-17）**新引入**的；
  只读生产库实测该 `rule_name` 在 `alerts` 表中**0 行** ⇒ 只能证明「**自该告警引入以来**未命中」，
  **不能**证明更早的历史未走过该支（那段时间的静默状态**无法回溯**）。

**⚠️ 「哨兵 `0`」这半句：结构缺陷已修复；但「数值后果」仍按旧版的收窄版引用**
- **结构上：已修复。** 哨兵由 `daily_return == 0`（**value** 判断）改为 `daily_return_computed`（**presence** 判断）
  ⇒ 「**没算出来**」与「**算出来恰好是 `0`**」现在**可以区分**，注释亦明写「`0.0` 一律保留」（`HEAD :757-761`）。
- **数值后果：不要按强版本引用。** 「真实 flat 日会被改走 `fallback`」这个后果在**新版已不成立**（哨兵不再看值）；
  而在**旧版**下它本身也几乎不可达 —— `daily_return` 由 `price_adj_mv / prev_common_mv` 得出，
  要**恰好**等于 `0` 需浮点**逐位相等**；**可达性边界**是 `common` 篮子退化为**价格完全不动**的标的
  （典型：只剩货基 `880013`，价恒 `1.0000` ⇒ `1.0 × qty == prev_mv` 可**精确**成立），
  而 34 只组合里不会只剩它一只 ⇒ 旧版也**不给**「会误判真 flat 日」这个结论。
- ⇒ **本条的主害从来不是数值错，而是「口径不可标识」**：读者/审计方**无法从 `portfolio_summary` 行**知道当天用的是哪个分母。
  **这一半只解决到「日志 + `alerts` 可查」，未解决到「行自带口径」。**

> ⇒ 「分母口径可反推」的**准确**表述：
> **折算闸门两支**可反推（日志文案）；**(v) 收窄**可反推（日志文案）；**`fallback`** 可反推（日志文案 ＋ `alerts` 表）；
> **但以上都不落在 `portfolio_summary` 行上 ⇒ 按行值反推仍不可执行。**

**⚠️ 两句话必须分开写（否则下一个读者会把「机制清楚了」读成「数就没有台阶了」）**

1. **机制已解**：`common` 定义域差异**能解释**那些「对不上」—— 这一条**已经结了**，不必再查。
2. **数据仍可能有台阶**：本节 §一~§六 的**份额折算污染是另一回事，与 `common` 口径无关**。
   折算日的 `daily_return` 仍会产出伪收益（§五 已证：**真拆分日必产伪 `-50%`，与快照是否入账无关**）。

> ⇒ **「`#91` 机制已解」≠「`daily_return` 干净」。** 前者是口径问题、已闭合；
> 后者是**数据**问题、仍是 §七 的未闭合项。

**⚠️ 另有一条独立缺陷：基期问题，与 `common` 无关（防止被并进来）**

`daily_return` 的**基期**取自 `portfolio_summary` **自身**（`HEAD` `43d06ce`：
`src/analysis/portfolio.py` 的 `:817-820`，其中 SQL 串位于 `:818`）：
`SELECT date FROM portfolio_summary WHERE date < ? ORDER BY date DESC LIMIT 1`，
**不是**上一交易日、**也不是**上一快照日 ⇒ **`portfolio_summary` 缺一天，它后一天就静默变成「两日收益」**，且盘上无标记。
实测命中两处：`2026-09-04`（summary 缺 `09-03`，实测**逐位精确**复现，基期 `09-02`）
与 `2026-09-16`（summary 缺 `09-15`）。
⇒ 这是**基期**问题，与 `common` 的**定义域**问题是两件事，**不要合并叙述**。

**⚠️ 而这条缺陷目前「不可取证」—— 这才是要立票的那一半**

**没落盘的其实是「三件套」，不是一件**（`verify-p1-batch` 2026-09-16 指出前两件，第三件见 (iv)）：

| # | 未落盘的东西 | 后果 |
|---|---|---|
| 1 | `daily_return` 实际用的 **`prev_dt`（基期日）** | 下游**无法反查基期** ⇒ 「引用前先反查基期」不可执行 |
| 2 | **`daily_return` 本身与它对应的口径** | `portfolio_summary` 只有一个裸数字，看不出算法（运行期键 `daily_return_coverage` **不落库**，见下） |
| 3 | **分母走了哪一支**（`common` / 收缩后的 `common` / `fallback` 全持仓 / (v) 的收窄层） | ⚠️ **部分闭合（2026-09-17）**：`fallback` 支**现在必留痕**（日志 ＋ `alerts` 表 `rule_name='daily_return_caliber_fallback'`），(v) 与折算闸门两支亦有日志文案 —— **但都不落在 `portfolio_summary` 行上** ⇒ 「按行值反推」仍不可执行 |

⇒ 「引用 `daily_return` 前必须反查基期」这条约定**在当前数据上不可执行** —— 下游拿不到基期，只能人读源码；
⇒ 而且**即便反查了基期也不够**，还要知道**当天用的是哪个分母 / 篮子**（(iv) 那支已可查但需跨表）。
⇒ 修法（**尚未排期**）：
1. `portfolio_summary` 补 **`return_base_date`** 列（最彻底，可直接当判据）；
2. 补 **`return_denom_kind`**（枚举：`common` / `common_minus_guard` / `fresh_basket` / `fallback_full`）—— 或至少在 `run_report` 里带出；
3. 至少在 `run_report` 里带出 `prev_dt`（改动最小，但只覆盖日报链路）。
   > ⚠️ 现状补充（逐行读到的代码事实）：运行期 `coverage` 字典**已经**带了 `freshness_source` / `included_n` /
   > `comparable_n` / `value_share` / `excluded_stale` / `caliber`（`HEAD :762-772` 与 `:930` / `:940`），
   > 并以 `'daily_return_coverage': coverage` 出现在 `_calculate_summary` 的返回里（`HEAD :1010`）——
   > **但它不是 `portfolio_summary` 的列**（只读生产库实测 16 列、无该列，2026-09-17）⇒ **同进程可见、跨进程不可见**（邮件路径即拿不到）。

⇒ 在补上之前，**凡引用 `daily_return` 的日期跨度，一律按「可能是多日收益」处理，不要假设它是一日；
也不要假设它的分母是 `common`。**

**⚠️ `portfolio_summary.daily_return` 还有第二个写入者、两套精度（与 `12_*` §17.5 对齐，本节不重复其数据）**

本节以上全部在讲 `_calculate_summary` 这一条写入路径。**但库里绝大多数 `daily_return` 不是它写的**：

- **逐行读到的代码事实**：`HEAD :1005-1006` —— `'daily_pnl': round(daily_pnl, 2),` / `'daily_return': round(daily_return, 2),`
  ⇒ **管线路径产出的 `daily_return` 必然是 ≤2 位小数**。
- `scripts/recompute_summary_window.py` 的 docstring 自述其口径用 `common_codes`（前后两日共同持仓）
  ⇒ **该口径没有「当日价新鲜」过滤**，与本节 (v) 的裁定 B **不同**。
  > **可 `grep` 原文**（`scripts/recompute_summary_window.py:14-17`，`HEAD` `43d06ce`，2026-09-17 由我逐行读取核实）：
  > `- 前 8 列（total_value / total_cost / total_pnl / daily_pnl / daily_return /`
  > `  profit_count / loss_count）：复刻 `src/utils/backfill.py:86-146`，`
  > `  用 common_codes（前后两日共同持仓、quantity 取前一日）算 daily_return，`
  > `  经 `audit/validate_algo.py` 验证可 30/30 复现现库旧值。`
- **调度事实**（⚠️ **转记 `12_*` §17.5，本节未重测**）：该脚本默认 dry-run、`scripts/` 内唯一引用它的文件是它自己、
  两个 `.bat` 均不含 `recompute` 字符串 ⇒ **它不在无人值守链上**。
  > 🔑 **出处声明**：本条是**转记**，不是本节实测。按约定 6（「数字必须来自真实执行」），
  > **本节不重复执行 glob 就不冒充实测**。需要复现请查 `12_*` §17.5 末段（那里是载体）。
- ⇒ **对本节的直接影响**：**`portfolio_summary.daily_return` 的精度本身就是「这行是谁写的」的旁证**
  （≤2 位 ⇒ 管线；全精度 ⇒ 非管线路径）。**引用本节口径时，必须先确认该行是谁写的**，
  否则会把「(v) 收窄口径」用到一行**根本不是该口径写的**数据上。
- ⛔ **不得据此断言「某历史行的值是错的」**：**口径不同 ≠ 错误**；裁定 B 只约束**新写入**，未回溯历史行。
  凡涉及「历史行该不该按 B 重算」的因果，属**未知定义**，**一律不写**（见 `12_*` §17.4 / §17.5 末条）。
- ⚠️ 本节的**行号与口径细节以 `12_*` §17.5 / §17.2 为准**（它们是载体）；本节只记「对 §八 的影响」，**不复制其数字**。

**⚠️ 引用源码时不要只写 `file:line` —— 行号会漂，而且「锚本身」也可能消失**

本节至今历经**三套行号体系**（`HEAD` `0d0b3b9` 系 → 工作区 `+234` 系 → **现行 `HEAD` `43d06ce` 系**）。
**这不是本节写得差，而是「一条引用的寿命由别人的提交决定」**。两个实测教训：

1. **行号漂移（会暴露）**：`verify-p1-batch` 2026-09-16 复核时，基期 SQL 在当时的在盘版本位于 `:502-505`；
   同一个旧行号 `:482` 在两版里指向**两个不同的东西**（`_conversion_qfq_ratio` 的 `return` vs 基期 `SELECT`）。
   ⇒ 这类错**会 `grep` 失败、当场暴露**，严重度可控。
2. 🔴 **锚本身消失（静默，更危险）**：`#115`（`7ee4cf5`）把
   `common_codes = set(curr_codes.keys()) & set(prev_snapshots.keys())` **改名为 `comparable_codes = …`**
   ⇒ 旧版锚点在 `HEAD` 里**已 `grep` 不到**。若当时只写行号、没写原文，读者会**跳到别的语句上而毫无提示**。
   ⇒ 本节因此确立：**每一处都必须有可 `grep` 的原文锚；行号只能在原文锚旁边作导航，不能是唯一定位手段。**

> 📌 **约定 9** 的**正式载体 = `11_measurement_conventions.md` §4**（team-lead 2026-09-16 裁定不再迁移）。
> 条文起草 = `verify-p1-batch`；本节旧版曾**转发**其全文与「差 `20` 的 `HEAD` 实测」= `risk-manager`。
> **该转发副本已按裁定压缩为本行指针**（避免两处口径漂移）；**引用约定 9 请引 `11_*` §4，不要引本节。**

---


