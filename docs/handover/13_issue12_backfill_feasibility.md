# 数据问题十二 · 回填可行性评估（消费侧 workstream · 仅评估，不动库）

> 配套交付：消费侧闸门已落地（见 `src/analysis/split_merge_guard.py` 及 `nav_engine / portfolio_risk / factor_attribution` 接线 + `tests/test_split_merge_guard.py` 14 例全绿）。
> 本文件**只做评估，不执行任何写库操作**。若后续需要回填快照行，必须另起任务、过完 §6 闸门后再动库。
> 证据口径与三族分类以 `07_known_data_issues.md`（问题十二）为准；本文件只补充"回填是否可行"的落地判断。

---

## 1. 范围与硬约束

| 项 | 取值 |
|---|---|
| 本 workstream 职责 | **消费侧 / 特征侧**闸门（Task 1，已交付）+ 回填可行性评估（Task 2，本文件） |
| **是否改 `daily_return` 公式** | **否** —— 公式侧属另一并行 workstream（`src/analysis/portfolio.py:503-541` 的 `common` 共同持仓法），本 workstream 不碰 |
| **是否写生产库** | **否** —— 本任务只评估；任何 `portfolio_snapshots` / `portfolio_summary` / `etf_features` / `portfolio_nav` 的写操作均**不在本任务范围** |
| 违反约束的处置 | 若发现"必须写库才能完成"，**立即 STOP 并上报**，不擅自写 |
| 生产库只读访问模式 | `sqlite3.connect("file:" + DATABASE_PATH + "?mode=ro", uri=True)`（已实测可用） |
| 备份要求 | 全量备份生产 `portfolio.db` 后再谈写；本评估阶段**不备份、不写** |

---

## 2. 当前实库状态（只读核验，2026-09-20 实跑）

读 `portfolio.db`（`mode=ro`）取 13 例在四张表的现状：

### 2.1 `portfolio_snapshots` 快照基准（quantity / current_price / market_value）

| 族 | code | date | qty | price | mv | 状态 |
|---|---|---|---|---|---|---|
| B（原记为 A，2026-09-20 重测归 B）| 510500 | 2015-04-15 | 659.87 | 7.818 | 5158.90 | qty 已折算、`mv` 连续（重测：`qty_ratio` 远离 1.0、`mv_ratio≈1.0`，无整倍放大）|
| B（原记为 A，2026-09-20 重测归 B）| 512010 | 2021-06-28 | 997081.34 | 0.836 | 833560.0 | qty 已折算、`mv` 连续（重测同上）|
| B（原记为 A，2026-09-20 重测归 B）| 512100 | 2022-09-05 | 1918.39 | 2.713 | 5204.60 | qty 已折算、`mv` 连续（重测同上）|
| B（原记为 A，2026-09-20 重测归 B）| 159300 | 2024-06-25 | 562.10 | 3.462 | 1946.0 | qty 已折算、`mv` 连续（重测同上）|
| B（原记为 A，2026-09-20 重测归 B）| 516160 | 2024-09-18 | 2489.67 | 1.597 | 3976.0 | qty 已折算、`mv` 连续（重测同上）|
| B | 512810 | 2025-06-23 | 50000.0 | 0.608 | 30400.0 | qty 已折算（×2）、mv 连续（✅ 快照无伪收益）|
| B | 159220 | 2025-11-10 | 20000.0 | 0.627 | 12540.0 | qty 已折算（×2）、mv 连续（✅）|
| C | 001323/001407/001437/519770 | 2026-06-30 | — | — | — | 场外月末估值切换（**跳过，不回填**）|
| C | 001765 | 2026-02-28 | 4754.34 | 2.493 | 11852.57 | 场外（**跳过**）|
| C | 166301 | 2026-07-31 | 5273.6 | 13.248 | 69864.65 | 场外（**跳过**）|

⇒ **订正（2026-09-20 重测，见 `07` §问题十二）**：原记为「A 族 5 例」的快照侧 `market_value` 经只读 SQL 重测，`qty_ratio` 全部远离 1.0、`mv_ratio ≈ 1.0`，份额已被正确折算、`market_value` 连续，**A 族实为 0 例**（无整倍放大、无需回填）。上述 5 例归并 B 族后，**B 族共 7 例快照均正确**；C 族 6 例性质未定、按 `07` §七 #1 必须跳过。

### 2.2 `portfolio_summary.daily_return`（**组合级别**，每日期一行，非按 code）

| code | date | 库内 `daily_return` | 说明 |
|---|---|---|---|
| 510500 | 2015-04-15 | **−0.84%** | 已非 §六 的 +248.55% 伪值 ⇒ 公式侧守卫已运行并修正 |
| 512010 | 2021-06-28 | **+0.96%** | 已非 −73.92% |
| 512100 | 2022-09-05 | **−0.71%** | 已非 +176.27% |
| 159300 | 2024-06-25 | **+0.25%** | 已非 +255.81% |
| 516160 | 2024-09-18 | **+1.95%** | 已非 +221.33% |
| 512810 | 2025-06-23 | **−4.53%** | 与 `07` §五 "失真值 −4.5286%" 一致（已修正）|
| 159220 | 2025-11-10 | **−1.52%** | 与 `07` §五 "失真值 −1.5150%" 一致（已修正）|
| 001323/001407/001437/519770 | 2026-06-30 | **+0.7487%**（四码同一日期=同一组合值）| 组合级，与单码无关 |
| 001765 | 2026-02-28 | **0.0%** | — |
| 166301 | 2026-07-31 | **+0.82%** | — |

⇒ **关键结论**：13 例的 `daily_return` 在实库里**已经是小值 / 已修正**（公式侧守卫 `portfolio.py` 已落地）。
⇒ 本 workstream 的闸门设计为 **"仅当 |stored daily_return| > 25% 才中性化、否则仅打标"**（`SPLIT_MERGE_NEUTRALIZE_DAILY_RETURN = 0.25`）——
这 13 例现存值全部 < 25% ⇒ **闸门不会误伤已修正的正确值**（盲目置零反而会劣化 −4.53% 这种合法小波动）。
⇒ 闸门的价值是**第二道防线**：覆盖未来可能的再污染（如历史盲区 `510500`/`512010` 行情表起点晚于快照，实时闸门 `CONVERSION_SUSPECT_RATIO` 管不到，见 `07` §五）。

### 2.3 `etf_features`（位置类 37 维）覆盖

| 族 | code/date | `etf_features` 行数 | 状态 |
|---|---|---|---|
| B（qfq 覆盖，2018+）| 6 例 | 各 1 行 | 已改用 qfq 合并价（`07` §附），位置特征连续化 |
| 510500（pre-2018，无 qfq，原记为 A、2026-09-20 重测归 B）| 2015-04-15 | 1 行 | **消费侧闸门兜底**：该日 ma5/ma20/ret 等整体置 NULL + `is_split_merge=1`（实测修复前 ma5=1.349、ma20=2.36 的台阶已消除）|
| C | 6 例 | 0 行 | 场外基金，**不在 `etf_features` 覆盖范围** |

⇒ C 族 6 例根本不进 `etf_features` ⇒ 再次证明 C 族是**另一数据类**（场外），回填与判别都必须把它们排除在外。
⇒ **新增（消费侧闸门延伸）**：`etf_features` 的 37 维特征也接了 `split_merge_guard`——
对「qfq 缺失的拆分/合并伪收益日」（典型 pre-2018，如 510500 2015，行情表 2018 才起、
价格源层仍含台阶）把当天位置/收益类特征置 NULL 并打 `is_split_merge` 标志，下游可据此排除。
见 `src/analysis/predictor/features.py` 的 `build_feature_matrix` 与 `tests/test_etf_features_split_guard.py`。

### 2.4 `portfolio_nav`

13 例日期各 1 行 NAV（已按 `daily_return` 累乘构建）。因 `daily_return` 已修正，NAV 序列目前**未被伪暴跌污染**。

---

## 3. 冲击力分析（blast radius）

> **订正（2026-09-20 重测）**：本节原基于「A 族 5 例快照侧 `market_value` 被整倍放大、需同步折算」的假设。经 `07` §问题十二 只读 SQL 重测，原 5 例 `qty_ratio` 全部远离 1.0、`mv_ratio ≈ 1.0`（份额已折算、`mv` 连续），**A 族实为 0 例**，原假设的「快照侧同步折算」操作**无对应行、不成立**。下表保留为历史参考（描述原假设场景的冲击面），但**不应据此产生任何写库动作**。

若（仅作历史假设推演）对原记为 A 族的 5 例执行"快照侧同步折算"，下游受影响面：

| 下游对象 | 是否受影响 | 影响性质 | 是否需全量重算 |
|---|---|---|---|
| `portfolio_snapshots.market_value`（原假设 A 族 5 行） | **否（已订正）** | A 族 = 0 例，无整倍放大的快照行；原「5 行 UPDATE」场景不成立 | 无 |
| `portfolio_summary` 权重 / 集中度（`max_weight` 等） | **否（已订正）** | 因 A 族快照 `mv` 连续、无需修正，权重分布不受本操作影响 | 无 |
| `portfolio_summary.daily_return` | **否（已修正）** | 公式侧守卫已落地；回填快照不会自动重算它，除非重跑 `portfolio.py`（重跑会再走守卫）| 视是否重跑而定 |
| `etf_features` 位置类 | **否** | 位置特征已改 qfq 合并价源（`07` §附）；pre-2018/no-qfq 拆分日再由消费侧闸门置 NULL+标记（`is_split_merge`），回填快照不影响它们 | 不需 |
| `etf_features.ret_1d` 等收益列 | **否（已处理）** | 收益列 qfq 覆盖处取真实日收益；qfq 缺失的拆分日由消费侧闸门置 NULL，不再含伪 ±200% | 不需 |
| `portfolio_nav` | **间接** | NAV 是 `daily_return` 的累积乘积；因 `daily_return` 已修正，NAV 当前无伪跌。若回填后重跑 `portfolio.py` 会重新生成 NAV，但守卫保证仍正确 | 仅当重跑时才全序列重算 |
| 历史全序列其它日期 | **否** | 仅 13 例、每标的一处 | — |

**结论**：A 族快照侧回填的**前提已不成立**——A 族 = 0 例，不存在需同步折算的整倍放大行；`daily_return` / NAV 因公式侧已修正而**天然免疫**。故本 workstream 没有任何「快照侧写库」的合法目标；最大不确定点仅剩 `etf_features.ret_1d` 是否已随公式侧修正（需先核实，不在此评估范围外推）。

---

## 4. 依赖关系

- **公式侧修复（另一 workstream）必须先于 / 同步于回填判定**：`07` §五 明确指出"回填修好快照侧，公式侧仍错"——两件事独立。本 workstream 已用消费侧闸门把公式侧失真在下游挡住（不依赖公式侧）。
- 回填快照**不会**自动修正 `daily_return`（它由 `portfolio.py` 计算且受守卫保护）。若回填后**不重跑** `portfolio.py`，`daily_return` 维持现状（已修正，安全）；若**重跑**，守卫保证仍正确。
- ⇒ 回填的"必要性"被公式侧守卫 + 本 workstream 闸门**大幅削弱**：消费侧的两道防线已让 13 例不再污染滚动窗口 / 波动 / 相关性 / 因子回归 / NAV 累乘。

---

## 5. 备份要求（写库前的硬性前置）

- 全量物理备份生产 `portfolio.db` 到独立路径（非 C: 盘旧备份、非工作区），校验 sha256 后再动。
- 写操作一律走 `mode=ro` 之外的独立连接；**本评估阶段不建立任何写连接**。
- 建议先在**备份副本**上跑一遍回填 + 下游重算 dry-run，diff 命中日期的 `portfolio_summary` 权重与 NAV，确认方向合理后再回灌生产。

---

## 6. 推荐决策

### 6.1 主推荐：**仅保留消费侧闸门，回填暂不做**（downstream-blocking-only）

理由：
1. **公式侧守卫已把 13 例 `daily_return` 修正为小值**（§2.2 实测），NAV / 风险 / 因子三大消费路径已不再吃到伪暴跌。
2. **本 workstream 闸门已落地**（§2 间接验证 + `tests/test_split_merge_guard.py` 14 例全绿 + `tests/test_etf_features_split_guard.py` 覆盖 37 维特征侧），对 nav_engine / portfolio_risk / factor_attribution 三处消费点做"检测 + 标记 + 极端值中性化"，并对 `etf_features` 的 pre-2018/no-qfq 拆分日置 NULL + `is_split_merge` 标志，把 `is_split_merge` 落库供审计。
3. **订正**：原以为回填的「唯一净收益」是修正 A 族 5 行的 `market_value` 与对应 `portfolio_summary` 权重；但 2026-09-20 重测确认 **A 族 = 0 例**（`mv` 连续、份额已折算），原 5 例已归 B 族。故**快照侧回填已无任何净收益目标**（不存在需修正的 A 族行），回填仅剩写库成本与回归风险，收益/风险比更低。
4. 回填有**写库 + 局部重算**的实操成本与回归风险，收益/风险比低。

### 6.2 若仍要做回填，必须过的闸门（checklist）

1. [ ] 公式侧修复已合并并验证（green）。
2. [ ] 生产库全量备份完成 + sha256 校验。
3. [ ] 在备份副本上 dry-run（若未来另有实证发现快照侧确有污染行）：`07` §问题十二 2026-09-20 重测已确认 A 族 = 0 例、原 5 例归 B 族且 `mv` 连续，本步骤当前无目标行 → 重算命中日期 `portfolio_summary` 权重 → diff 前后 NAV。
4. [ ] 核实 `etf_features.ret_1d` 当前是否已随公式侧修正（避免重复污染）。
5. [ ] C 族 6 例**明确排除**在回填之外（性质未定，见 `07` §七 #1）。
6. [ ] 回填后跑回归：`test_split_merge_guard` + `test_risk` + `test_regression_db_isolation` 全绿。
7. [ ] （当前无回填目标行）若未来确需回填，只 `UPDATE` 显式命中的行，**严禁** `git add -A` / 全表扫描式写。

### 6.3 风险提示

- `510500`/`512010`（pre-2018 / 无 qfq 的拆分日，2026-09-20 重测已归 B 族）处于实时折算闸门盲区（行情表起点晚于快照），未来若重跑 `portfolio.py` 且无守卫兜底会再污染——本 workstream 闸门是唯一兜底，切忌在移除前确认守卫已生效。
- `market_value` 是多处（报表、权重、集中度）的**共同源**，快照 `mv` 经重测已连续（A 族 = 0 例），请同步核对任何"直接读快照 mv"的报表口径，确保口径与实库一致。

---

## 7. 结论

- **回填在技术上可行，但当前无目标行**：经 2026-09-20 重测，**A 族 = 0 例**（原记为 A 的 5 例已归 B 族、份额已折算、`mv` 连续），不存在需改的 A 族快照行；`daily_return`/NAV 因公式侧已修正而免疫。原「仅 5 行 A 族快照需改」的论断已作废。
- **但当前不必做**：消费侧两道防线（公式侧守卫 + 本 workstream 闸门）已消除 13 例对分析产物的实际污染，回填的边际收益低、且引入写库风险。
- **建议**：保持下游闸门为长期防线；回填列入"待公式侧完全稳定 + 备份就绪后"的可选项，按 §6.2 闸门执行。本 workstream 不写库、不负责任何回填写操作。
